---
phase: 23-uat-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/dev/notyouraverage/smscourier/composables/screens/PairedDevicesScreen.kt
  - app/src/main/java/dev/notyouraverage/smscourier/composables/components/SessionHistoryComponents.kt
autonomous: true

must_haves:
  truths:
    - "Settings screen opens without crash when clicking settings icon"
    - "Paired Devices screen opens without crash when clicking card"
    - "Devices in 'I Forward To' tab do not crash when clicked"
    - "Session History long-press export works without crash"
  artifacts:
    - path: "app/src/main/java/dev/notyouraverage/smscourier/composables/screens/PairedDevicesScreen.kt"
      provides: "DeviceCard with fixed combinedClickable"
      contains: "interactionSource = remember"
    - path: "app/src/main/java/dev/notyouraverage/smscourier/composables/components/SessionHistoryComponents.kt"
      provides: "SessionCard with fixed combinedClickable"
      contains: "interactionSource = remember"
  key_links:
    - from: "DeviceCard"
      to: "combinedClickable modifier"
      via: "explicit indication parameter"
      pattern: "LocalIndication.current"
---

<objective>
Fix combinedClickable crashes that occur when clicking various UI elements due to deprecated Indication API.

Purpose: App crashes immediately when users tap Settings, Paired Devices card, or device entries due to incompatible combinedClickable API. This is a blocking crash that prevents normal app usage.

Output: Both affected files use the correct combinedClickable overload that explicitly passes LocalIndication.current and a MutableInteractionSource.
</objective>

<execution_context>
@/Users/aanand/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aanand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/src/main/java/dev/notyouraverage/smscourier/composables/screens/PairedDevicesScreen.kt
@app/src/main/java/dev/notyouraverage/smscourier/composables/components/SessionHistoryComponents.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix combinedClickable in PairedDevicesScreen.kt</name>
  <files>app/src/main/java/dev/notyouraverage/smscourier/composables/screens/PairedDevicesScreen.kt</files>
  <action>
    Update the DeviceCard composable (around line 363-370) to fix the combinedClickable modifier.

    1. Add import for LocalIndication: `import androidx.compose.foundation.LocalIndication`
    2. Add import for MutableInteractionSource: `import androidx.compose.foundation.interaction.MutableInteractionSource`

    3. Change the combinedClickable modifier from:
    ```kotlin
    .combinedClickable(
        onClick = onClick,
        onLongClick = onLongClick,
    )
    ```

    To:
    ```kotlin
    .combinedClickable(
        interactionSource = remember { MutableInteractionSource() },
        indication = LocalIndication.current,
        onClick = onClick,
        onLongClick = onLongClick,
    )
    ```

    The fix adds explicit interactionSource and indication parameters which are required by newer Compose Foundation versions.
  </action>
  <verify>
    Build with `./gradlew assembleDebug` - should compile without errors.
    Grep for the pattern: `grep -n "interactionSource = remember" app/src/main/java/dev/notyouraverage/smscourier/composables/screens/PairedDevicesScreen.kt`
  </verify>
  <done>DeviceCard's combinedClickable uses explicit interactionSource and LocalIndication.current</done>
</task>

<task type="auto">
  <name>Task 2: Fix combinedClickable in SessionHistoryComponents.kt</name>
  <files>app/src/main/java/dev/notyouraverage/smscourier/composables/components/SessionHistoryComponents.kt</files>
  <action>
    Update the SessionCard composable (around line 63-69) to fix the combinedClickable modifier.

    1. Add import for LocalIndication: `import androidx.compose.foundation.LocalIndication`
    2. Add import for MutableInteractionSource: `import androidx.compose.foundation.interaction.MutableInteractionSource`

    3. Change the combinedClickable modifier from:
    ```kotlin
    .combinedClickable(
        onClick = onClick,
        onLongClick = { showMenu = true },
    )
    ```

    To:
    ```kotlin
    .combinedClickable(
        interactionSource = remember { MutableInteractionSource() },
        indication = LocalIndication.current,
        onClick = onClick,
        onLongClick = { showMenu = true },
    )
    ```
  </action>
  <verify>
    Build with `./gradlew assembleDebug` - should compile without errors.
    Grep for the pattern: `grep -n "interactionSource = remember" app/src/main/java/dev/notyouraverage/smscourier/composables/components/SessionHistoryComponents.kt`
  </verify>
  <done>SessionCard's combinedClickable uses explicit interactionSource and LocalIndication.current</done>
</task>

</tasks>

<verification>
1. Build succeeds: `./gradlew assembleDebug`
2. Run spotless check: `./gradlew spotlessCheck` (should pass after `spotlessApply`)
3. Both files have the fixed combinedClickable pattern with LocalIndication.current
4. No other combinedClickable usages exist in codebase (grep confirms only 2 files)
</verification>

<success_criteria>
- Build succeeds without errors
- Both PairedDevicesScreen.kt and SessionHistoryComponents.kt use the fixed combinedClickable pattern
- App does not crash when clicking Settings icon, Paired Devices card, device entries, or session cards
</success_criteria>

<output>
After completion, create `.planning/phases/23-uat-bug-fixes/23-01-SUMMARY.md`
</output>
