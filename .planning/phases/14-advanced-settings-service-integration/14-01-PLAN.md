---
phase: 14-advanced-settings-service-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/dev/notyouraverage/smscourier/viewmodels/SettingsViewModel.kt
  - app/src/main/java/dev/notyouraverage/smscourier/composables/screens/SettingsScreen.kt
autonomous: true

must_haves:
  truths:
    - "User can access Advanced section in Settings"
    - "User can configure lockout duration (1-60 minutes)"
    - "User can configure max failed attempts (1-10)"
    - "User can configure challenge expiry (1-10 minutes)"
    - "User can configure max pairing resend attempts (1-10)"
    - "User can configure pairing resend cooldown (1-10 minutes)"
    - "User can configure auth request timeout (1-30 minutes)"
    - "Invalid input shows error message"
  artifacts:
    - path: "app/src/main/java/dev/notyouraverage/smscourier/viewmodels/SettingsViewModel.kt"
      provides: "StateFlows and setters for 6 security settings"
      contains: "lockoutDurationMinutes"
    - path: "app/src/main/java/dev/notyouraverage/smscourier/composables/screens/SettingsScreen.kt"
      provides: "Advanced section UI with number input items"
      contains: "SettingsNumberInputItem"
  key_links:
    - from: "SettingsScreen.kt"
      to: "SettingsViewModel"
      via: "collectAsState for security settings"
      pattern: "viewModel\\.lockoutDurationMinutes"
    - from: "SettingsViewModel.kt"
      to: "SettingsRepository"
      via: "settingsRepository Flow calls"
      pattern: "settingsRepository\\.lockoutDurationMinutes"
---

<objective>
Add Advanced Settings section to SettingsScreen exposing 6 security configuration parameters.

Purpose: Enable users to customize security timing parameters (ADV-01 through ADV-06). These settings exist in SettingsRepository but are not yet exposed in the UI.

Output: Collapsible Advanced section with freeform numeric inputs for each security setting, with validation and error display.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-advanced-settings-service-integration/14-CONTEXT.md
@.planning/phases/14-advanced-settings-service-integration/14-RESEARCH.md

@app/src/main/java/dev/notyouraverage/smscourier/viewmodels/SettingsViewModel.kt
@app/src/main/java/dev/notyouraverage/smscourier/composables/screens/SettingsScreen.kt
@app/src/main/java/dev/notyouraverage/smscourier/repository/SettingsRepository.kt
@app/src/main/java/dev/notyouraverage/smscourier/data/settings/SettingsDefaults.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend SettingsViewModel with security settings</name>
  <files>app/src/main/java/dev/notyouraverage/smscourier/viewmodels/SettingsViewModel.kt</files>
  <action>
Add StateFlows and setter methods for all 6 security settings:

1. Add StateFlows (following existing pattern with stateIn):
   - lockoutDurationMinutes: StateFlow<Int> (default: SettingsDefaults.LOCKOUT_DURATION)
   - maxFailedAttempts: StateFlow<Int> (default: SettingsDefaults.MAX_FAILED_ATTEMPTS)
   - challengeExpiryMinutes: StateFlow<Int> (default: SettingsDefaults.CHALLENGE_EXPIRY)
   - maxPairingResendAttempts: StateFlow<Int> (default: SettingsDefaults.MAX_PAIRING_RESEND_ATTEMPTS)
   - pairingResendCooldownMinutes: StateFlow<Int> (default: SettingsDefaults.PAIRING_RESEND_COOLDOWN)
   - authRequestTimeoutMinutes: StateFlow<Int> (default: SettingsDefaults.AUTH_REQUEST_TIMEOUT)

2. Add setter methods that call repository in viewModelScope.launch:
   - setLockoutDuration(minutes: Int)
   - setMaxFailedAttempts(attempts: Int)
   - setChallengeExpiry(minutes: Int)
   - setMaxPairingResendAttempts(attempts: Int)
   - setPairingResendCooldown(minutes: Int)
   - setAuthRequestTimeout(minutes: Int)

3. Each setter should wrap the repository call in try/catch and return Result<Unit> or set an error StateFlow. Repository throws IllegalArgumentException on invalid input.

4. Add error state for validation feedback:
   - private val _settingError = MutableStateFlow<String?>(null)
   - val settingError: StateFlow<String?> = _settingError.asStateFlow()
   - fun clearSettingError() to reset

5. Import SettingsDefaults for default values.

Follow existing code patterns from the current SettingsViewModel.
  </action>
  <verify>
Build succeeds: `./gradlew assembleDebug`
SettingsViewModel compiles with new StateFlows and setters.
  </verify>
  <done>
SettingsViewModel exposes 6 security settings via StateFlows with validation-aware setters.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Advanced section to SettingsScreen</name>
  <files>app/src/main/java/dev/notyouraverage/smscourier/composables/screens/SettingsScreen.kt</files>
  <action>
Add collapsible Advanced section with numeric input controls:

1. Create SettingsNumberInputItem composable (private):
```kotlin
@Composable
private fun SettingsNumberInputItem(
    title: String,
    value: Int,
    onValueChange: (Int) -> Unit,
    minValue: Int,
    maxValue: Int,
    unit: String, // "minutes" or "attempts"
    helperText: String? = null, // For contextual warnings
)
```
   - Use ListItem with OutlinedTextField (width ~80.dp) in trailingContent
   - Show range in supportingContent: "$minValue-$maxValue $unit"
   - If helperText provided, show below range in secondary color
   - KeyboardOptions(keyboardType = KeyboardType.Number)
   - Parse input with toIntOrNull(), only call onValueChange if valid integer
   - singleLine = true

2. Add expand/collapse state for Advanced section:
   - var advancedExpanded by remember { mutableStateOf(false) }

3. Add Advanced section between Preferences and Information:
   - SectionHeader with expand/collapse icon (KeyboardArrowDown when collapsed, KeyboardArrowUp when expanded)
   - Make header clickable to toggle advancedExpanded
   - Use AnimatedVisibility(visible = advancedExpanded) to wrap content

4. Inside AnimatedVisibility, add warning header:
```kotlin
ListItem(
    headlineContent = {
        Text(
            "These settings affect security. Change with care.",
            color = MaterialTheme.colorScheme.error,
            style = MaterialTheme.typography.bodySmall,
        )
    },
    leadingContent = {
        Icon(
            Icons.Default.Warning,
            contentDescription = null,
            tint = MaterialTheme.colorScheme.error,
        )
    },
)
```

5. Add numeric input items (grouped as Security, then Pairing):
   Security:
   - Lockout duration (1-60 min): "How long devices are locked after failed auth"
   - Max failed attempts (1-10): "Failed attempts before lockout"
   - Challenge expiry (1-10 min): "Time before auth challenge expires"

   Pairing:
   - Max pairing resend (1-10): "Maximum resend attempts"
   - Pairing resend cooldown (1-10 min): "Wait time between resends"
   - Auth request timeout (1-30 min): "Time before auth request expires"

6. Collect new StateFlows from viewModel:
```kotlin
val lockoutDuration by viewModel.lockoutDurationMinutes.collectAsState()
val maxFailedAttempts by viewModel.maxFailedAttempts.collectAsState()
// ... etc
```

7. Add imports: AnimatedVisibility, KeyboardArrowUp, KeyboardOptions, KeyboardType, OutlinedTextField, width, dp

8. Show error snackbar if settingError is non-null (optional - if time permits).
  </action>
  <verify>
Build succeeds: `./gradlew assembleDebug`
App launches, Settings screen shows Advanced section (collapsed by default).
Expanding shows warning and 6 numeric inputs.
  </verify>
  <done>
Settings screen displays collapsible Advanced section with all 6 security settings as freeform numeric inputs.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run spotless and verify build</name>
  <files>app/src/main/java/dev/notyouraverage/smscourier/viewmodels/SettingsViewModel.kt, app/src/main/java/dev/notyouraverage/smscourier/composables/screens/SettingsScreen.kt</files>
  <action>
1. Run `./gradlew spotlessApply` to format code
2. Run `./gradlew assembleDebug` to verify build
3. Run `./gradlew test` to ensure no existing tests break
  </action>
  <verify>
All commands succeed without errors.
  </verify>
  <done>
Code is formatted and all builds/tests pass.
  </done>
</task>

</tasks>

<verification>
1. `./gradlew assembleDebug` succeeds
2. `./gradlew test` passes (342+ tests)
3. App launches, navigate to Settings
4. Advanced section appears between Preferences and Information
5. Advanced section starts collapsed
6. Clicking header expands section with warning and 6 inputs
7. Each input shows correct current value
8. Each input shows valid range in supporting text
9. Typing valid values updates immediately (Flow reactive)
10. Typing invalid values (out of range or non-numeric) shows error or is rejected
</verification>

<success_criteria>
- SettingsViewModel exposes 6 security settings via StateFlows
- SettingsScreen has collapsible Advanced section with warning header
- All 6 security settings have freeform numeric input controls
- Input validation shows ranges and rejects invalid values
- Build and all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/14-advanced-settings-service-integration/14-01-SUMMARY.md`
</output>
