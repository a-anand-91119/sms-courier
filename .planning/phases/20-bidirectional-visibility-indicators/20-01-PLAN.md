---
phase: 20-bidirectional-visibility-indicators
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/dev/notyouraverage/smscourier/data/Direction.kt
  - app/src/main/java/dev/notyouraverage/smscourier/viewmodels/HomeViewModel.kt

autonomous: true

must_haves:
  truths:
    - "HomeViewModel calculates active session direction based on DeviceRole"
    - "Direction enum exists with FORWARDING_TO, RECEIVING_FROM, BIDIRECTIONAL values"
    - "DirectionalStatus data class tracks counts per direction and active sessions"
    - "HomeState exposes directionalStatus for UI consumption"
  artifacts:
    - path: "app/src/main/java/dev/notyouraverage/smscourier/data/Direction.kt"
      provides: "Direction enum and DirectionalStatus data class"
      exports: ["Direction", "DirectionalStatus", "SessionWithDirection"]
    - path: "app/src/main/java/dev/notyouraverage/smscourier/viewmodels/HomeViewModel.kt"
      provides: "Extended HomeState with directionalStatus"
      contains: "directionalStatus"
  key_links:
    - from: "HomeViewModel"
      to: "Direction enum"
      via: "import and calculateDirectionalStatus"
      pattern: "Direction\\.(FORWARDING_TO|RECEIVING_FROM|BIDIRECTIONAL)"
---

<objective>
Add Direction enum, DirectionalStatus data class, and directional status calculation to HomeViewModel.

Purpose: Provides the data layer for directional indicators showing whether user is forwarding TO, receiving FROM, or bidirectional with paired devices.

Output: Direction.kt with data models, HomeViewModel extended with directionalStatus in HomeState.
</objective>

<execution_context>
@/Users/aanand/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aanand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-bidirectional-visibility-indicators/20-RESEARCH.md
@app/src/main/java/dev/notyouraverage/smscourier/data/entities/PairedDevice.kt
@app/src/main/java/dev/notyouraverage/smscourier/data/entities/ForwardingSession.kt
@app/src/main/java/dev/notyouraverage/smscourier/viewmodels/HomeViewModel.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Direction enum and data classes</name>
  <files>app/src/main/java/dev/notyouraverage/smscourier/data/Direction.kt</files>
  <action>
Create new file Direction.kt in the data package with:

1. **Direction enum** with three values:
   - FORWARDING_TO: This device is SOURCE (requesting/receiving forwarded messages ↓)
   - RECEIVING_FROM: This device is TARGET (providing/sending forwarded messages ↑)
   - BIDIRECTIONAL: Both directions active with same phone number

2. **SessionWithDirection data class**:
   - session: ForwardingSession
   - device: PairedDevice
   - direction: Direction

3. **DirectionalStatus data class**:
   - forwardingToCount: Int = 0 (this device receives from others)
   - receivingFromCount: Int = 0 (this device sends to others)
   - bidirectionalCount: Int = 0 (both directions active)
   - activeSessions: List<SessionWithDirection> = emptyList()

Include package declaration and necessary imports (PairedDevice, ForwardingSession).

**Critical semantic note (from RESEARCH.md):**
- SOURCE devices REQUEST forwarding = they RECEIVE messages (↓ FORWARDING_TO)
- TARGET devices PROVIDE forwarding = they SEND messages (↑ RECEIVING_FROM)
- This mental model MUST be documented in comments to prevent role inversion.
  </action>
  <verify>File exists at app/src/main/java/dev/notyouraverage/smscourier/data/Direction.kt and compiles without errors: ./gradlew compileDebugKotlin</verify>
  <done>Direction.kt exists with Direction enum (3 values), SessionWithDirection, and DirectionalStatus data classes with correct semantic documentation</done>
</task>

<task type="auto">
  <name>Task 2: Add directional status calculation to HomeViewModel</name>
  <files>app/src/main/java/dev/notyouraverage/smscourier/viewmodels/HomeViewModel.kt</files>
  <action>
Extend HomeViewModel to calculate directional status:

1. **Import Direction classes**:
   - import dev.notyouraverage.smscourier.data.Direction
   - import dev.notyouraverage.smscourier.data.DirectionalStatus
   - import dev.notyouraverage.smscourier.data.SessionWithDirection

2. **Add directionalStatus to HomeState**:
   ```kotlin
   data class HomeState(
       val approvedDevicesCount: Int = 0,
       val pendingRequestsCount: Int = 0,
       val activeSessionsCount: Int = 0,
       val activeSessions: List<ForwardingSession> = emptyList(),
       val directionalStatus: DirectionalStatus = DirectionalStatus()
   )
   ```

3. **Create calculateDirectionalStatus private function**:
   Takes approved devices (List<PairedDevice>) and active sessions (List<ForwardingSession>).

   Logic (from RESEARCH.md Pattern 1):
   - Group sessions by devicePhoneNumber
   - For each phone with active sessions:
     - Find paired devices with that phone number
     - Check if device has SOURCE role (forwardingTo) and/or TARGET role (receivingFrom)
     - If BOTH roles exist for same phone with active sessions → bidirectional
     - If only SOURCE → forwardingTo (this device receives)
     - If only TARGET → receivingFrom (this device sends)
   - Build SessionWithDirection list and count each category

   **Critical logic clarification:**
   - Bidirectional requires active sessions for BOTH roles with same phone
   - Not just both roles existing, but actual active sessions in both directions

4. **Update combine() in homeState**:
   Call calculateDirectionalStatus(approved, sessions) and include result in HomeState.

Do NOT change existing functionality for approvedDevicesCount, pendingRequestsCount, activeSessionsCount.
  </action>
  <verify>Build succeeds: ./gradlew compileDebugKotlin && verify HomeState now has directionalStatus field</verify>
  <done>HomeViewModel calculates and exposes directionalStatus in HomeState, correctly categorizing sessions as FORWARDING_TO, RECEIVING_FROM, or BIDIRECTIONAL based on DeviceRole</done>
</task>

</tasks>

<verification>
1. ./gradlew compileDebugKotlin passes without errors
2. Direction.kt exists with Direction enum (3 values), SessionWithDirection, DirectionalStatus
3. HomeViewModel.kt contains directionalStatus in HomeState
4. HomeViewModel.kt contains calculateDirectionalStatus function
5. Run spotlessCheck: ./gradlew spotlessCheck (apply if needed: ./gradlew spotlessApply)
</verification>

<success_criteria>
- Direction enum with FORWARDING_TO, RECEIVING_FROM, BIDIRECTIONAL exists
- DirectionalStatus data class with counts and sessions list exists
- HomeState includes directionalStatus field
- HomeViewModel calculates directional status from approved devices and active sessions
- Build compiles without errors
- Spotless formatting passes
</success_criteria>

<output>
After completion, create `.planning/phases/20-bidirectional-visibility-indicators/20-01-SUMMARY.md`
</output>
