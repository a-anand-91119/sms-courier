---
phase: 11-e2e-flow-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/test/java/dev/notyouraverage/smscourier/integration/PairingFlowIntegrationTest.kt
autonomous: true

must_haves:
  truths:
    - "Pairing request from unknown device creates TARGET with PENDING_RECEIVED status"
    - "Pairing approval updates status to APPROVED and sends PAIR_APPROVED SMS"
    - "Pairing rejection updates status to REJECTED and sends PAIR_REJECTED SMS"
    - "Unpair deletes device and sends UNPAIR confirmation"
    - "Already paired device handles duplicate pairing request gracefully"
    - "Bidirectional pairing creates both SOURCE and TARGET roles"
  artifacts:
    - path: "app/src/test/java/dev/notyouraverage/smscourier/integration/PairingFlowIntegrationTest.kt"
      provides: "Pairing flow integration tests"
      min_lines: 200
  key_links:
    - from: "PairingFlowIntegrationTest"
      to: "SmsCommandHandler.handlePairRequest"
      via: "commandHandler.handlePairRequest()"
      pattern: "commandHandler\\.handlePairRequest"
    - from: "PairingFlowIntegrationTest"
      to: "SmsCommandHandler.approvePairing"
      via: "commandHandler.approvePairing()"
      pattern: "commandHandler\\.approvePairing"
---

<objective>
Create integration tests for the complete pairing flow covering request, approval, rejection, and unpair scenarios.

Purpose: Validate that pairing commands work end-to-end with database state changes and correct SMS responses.
Output: PairingFlowIntegrationTest.kt with comprehensive pairing flow coverage.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-e2e-flow-tests/11-CONTEXT.md
@.planning/phases/11-e2e-flow-tests/11-RESEARCH.md

Infrastructure files:
@app/src/test/java/dev/notyouraverage/smscourier/integration/IntegrationTestBase.kt
@app/src/test/java/dev/notyouraverage/smscourier/integration/ScenarioBuilders.kt
@app/src/test/java/dev/notyouraverage/smscourier/integration/CapturingSmsSender.kt
@app/src/test/java/dev/notyouraverage/smscourier/integration/InfrastructureValidationTest.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PairingFlowIntegrationTest with pairing request and approval tests</name>
  <files>app/src/test/java/dev/notyouraverage/smscourier/integration/PairingFlowIntegrationTest.kt</files>
  <action>
Create PairingFlowIntegrationTest.kt extending IntegrationTestBase with:

CRITICAL: Do NOT use @Nested classes. JUnit 4 does not support them. Use comment-based grouping instead.

Class structure:
```kotlin
@IntegrationTest
@RunWith(RobolectricTestRunner::class)
@Config(sdk = [34])
@ExperimentalCoroutinesApi
class PairingFlowIntegrationTest : IntegrationTestBase() {
    // Tests grouped by comment sections
}
```

Tests to implement (use backtick method names for readability):

// ==================== Pairing Request Tests ====================

1. `givenUnknownDevice_whenPairRequestReceived_thenTargetCreatedWithPendingReceived`
   - No setup (unknown device)
   - Call commandHandler.handlePairRequest("+1234567890")
   - waitForAsync()
   - Verify: deviceRepository has TARGET with PENDING_RECEIVED status

2. `givenAlreadyPairedDevice_whenPairRequestReceived_thenNoDuplicateCreated`
   - Setup: setupApprovedTargetDevice("+1234567890")
   - Call commandHandler.handlePairRequest("+1234567890")
   - waitForAsync()
   - Verify: Still one device, status unchanged (APPROVED)

3. `givenPendingSentDevice_whenPairRequestReceived_thenBothRolesExist`
   - Setup: setupPendingSent("+1234567890") (we sent request to them)
   - Call commandHandler.handlePairRequest("+1234567890") (they sent request to us)
   - waitForAsync()
   - Verify: Both SOURCE (PENDING_SENT) and TARGET (PENDING_RECEIVED) exist

// ==================== Pairing Approval Tests ====================

4. `givenPendingReceivedDevice_whenApproved_thenStatusApprovedAndSmsSent`
   - Setup: setupPendingPairingRequest("+1234567890")
   - Call commandHandler.approvePairing("+1234567890", "password123")
   - waitForAsync()
   - Verify: Status is APPROVED, passwordHash set, authKey set
   - Verify: capturingSmsSender.assertSent("+1234567890", Regex("SMSC PAIR_APPROVED"))

5. `givenPendingReceivedDevice_whenApproved_thenPasswordHashedWithBcrypt`
   - Setup: setupPendingPairingRequest("+1234567890")
   - Call commandHandler.approvePairing("+1234567890", "password123")
   - waitForAsync()
   - Verify: passwordHash starts with "$2" (bcrypt format)
   - Verify: authKey is not null and not the raw password

// ==================== Pairing Rejection Tests ====================

6. `givenPendingReceivedDevice_whenRejected_thenStatusRejectedAndSmsSent`
   - Setup: setupPendingPairingRequest("+1234567890")
   - Call commandHandler.rejectPairing("+1234567890")
   - waitForAsync()
   - Verify: Status is REJECTED
   - Verify: capturingSmsSender.assertSent("+1234567890", Regex("SMSC PAIR_REJECTED"))

Use ScenarioBuilders for setup, follow InfrastructureValidationTest patterns.
  </action>
  <verify>./gradlew test --tests "*PairingFlowIntegrationTest*" passes</verify>
  <done>6 pairing request/approval/rejection tests passing</done>
</task>

<task type="auto">
  <name>Task 2: Add unpair and bidirectional pairing tests</name>
  <files>app/src/test/java/dev/notyouraverage/smscourier/integration/PairingFlowIntegrationTest.kt</files>
  <action>
Add additional tests to PairingFlowIntegrationTest.kt:

// ==================== Unpair Tests ====================

7. `givenApprovedTargetDevice_whenUnpairSourceReceived_thenDeviceDeleted`
   - Setup: setupApprovedTargetDevice("+1234567890")
   - Call commandHandler.handleUnpair("+1234567890", DeviceRole.SOURCE)
   - waitForAsync()
   - Verify: Device with SOURCE role deleted, TARGET unchanged (or verify based on actual command logic)

8. `givenApprovedSourceDevice_whenUnpairTargetReceived_thenDeviceDeleted`
   - Setup: setupApprovedSourceDevice("+1234567890")
   - Call commandHandler.handleUnpair("+1234567890", DeviceRole.TARGET)
   - waitForAsync()
   - Verify: Device with TARGET role deleted

9. `givenNoDevice_whenUnpairReceived_thenNoError`
   - No setup
   - Call commandHandler.handleUnpair("+9999999999", DeviceRole.SOURCE)
   - waitForAsync()
   - Verify: No exception thrown, no SMS sent (graceful handling)

// ==================== Bidirectional Pairing Tests ====================

10. `givenBidirectionalPairing_whenUnpairSourceReceived_thenOnlySourceRoleDeleted`
    - Setup: setupBidirectionalPairing("+1234567890")
    - Call commandHandler.handleUnpair("+1234567890", DeviceRole.SOURCE)
    - waitForAsync()
    - Verify: SOURCE role deleted, TARGET role still exists

11. `givenBidirectionalPairing_whenUnpairTargetReceived_thenOnlyTargetRoleDeleted`
    - Setup: setupBidirectionalPairing("+1234567890")
    - Call commandHandler.handleUnpair("+1234567890", DeviceRole.TARGET)
    - waitForAsync()
    - Verify: TARGET role deleted, SOURCE role still exists

// ==================== Full Pairing Flow Tests ====================

12. `fullPairingFlow_requestThenApprove_thenUnpair`
    - Step 1: commandHandler.handlePairRequest("+1234567890")
    - Checkpoint: Device created with PENDING_RECEIVED
    - Step 2: commandHandler.approvePairing("+1234567890", "password123")
    - Checkpoint: Status APPROVED, PAIR_APPROVED sent
    - Step 3: commandHandler.handleUnpair("+1234567890", DeviceRole.SOURCE)
    - Verify: Device deleted

Import DeviceRole for role-specific unpair tests.
  </action>
  <verify>./gradlew test --tests "*PairingFlowIntegrationTest*" passes (all 12+ tests)</verify>
  <done>12+ pairing flow tests passing, covering request, approve, reject, unpair, and bidirectional scenarios</done>
</task>

</tasks>

<verification>
- All tests pass: `./gradlew test --tests "*PairingFlowIntegrationTest*"`
- Full integration test suite: `./gradlew test --tests "*Integration*"`
- Test file exists and has 200+ lines
- No @Nested annotations present (JUnit 4 compatible)
- Tests use ScenarioBuilders for state setup
- Tests use CapturingSmsSender for SMS verification
</verification>

<success_criteria>
- PairingFlowIntegrationTest.kt exists with 12+ tests
- Tests cover: pairing request, approval, rejection, unpair, bidirectional
- All tests pass
- Tests follow Given-When-Then naming convention
- Tests use comment-based grouping (not @Nested)
</success_criteria>

<output>
After completion, create `.planning/phases/11-e2e-flow-tests/11-01-SUMMARY.md`
</output>
