---
phase: 11-e2e-flow-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/test/java/dev/notyouraverage/smscourier/integration/PairingFlowIntegrationTest.kt
autonomous: true

must_haves:
  truths:
    - "Pairing request from unknown device creates pending state and notifies user"
    - "Pairing approval updates device status and sends PAIR_APPROVED command"
    - "Pairing rejection updates device status and sends PAIR_REJECTED command"
    - "Unpair command removes correct device based on role specifier"
    - "Already paired device cannot be paired again without unpair"
    - "Bidirectional pairing creates both SOURCE and TARGET roles"
  artifacts:
    - path: "app/src/test/java/dev/notyouraverage/smscourier/integration/PairingFlowIntegrationTest.kt"
      provides: "Complete pairing flow integration tests"
      min_lines: 200
      contains: "@IntegrationTest"
  key_links:
    - from: "PairingFlowIntegrationTest"
      to: "commandHandler"
      via: "handleCommand/handlePairRequest/approvePairing/rejectPairing/handleUnpair"
      pattern: "commandHandler\\."
    - from: "PairingFlowIntegrationTest"
      to: "deviceRepository"
      via: "state verification"
      pattern: "deviceRepository\\."
---

<objective>
Create comprehensive integration tests for the complete pairing flow.

Purpose: Verify all pairing scenarios work end-to-end through real commandHandler with real database state changes.
Output: PairingFlowIntegrationTest.kt with tests for request, approve, reject, unpair, and edge cases.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-e2e-flow-tests/11-CONTEXT.md

# Phase 10 infrastructure (what we're building on)
@app/src/test/java/dev/notyouraverage/smscourier/integration/IntegrationTestBase.kt
@app/src/test/java/dev/notyouraverage/smscourier/integration/ScenarioBuilders.kt
@app/src/test/java/dev/notyouraverage/smscourier/integration/CapturingSmsSender.kt
@app/src/test/java/dev/notyouraverage/smscourier/integration/InfrastructureValidationTest.kt

# Source code being tested
@app/src/main/java/dev/notyouraverage/smscourier/handlers/SmsCommandHandler.kt
@app/src/main/java/dev/notyouraverage/smscourier/commands/CommandPatterns.kt
@app/src/main/java/dev/notyouraverage/smscourier/commands/CommandParser.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PairingFlowIntegrationTest with happy path and approval/rejection tests</name>
  <files>app/src/test/java/dev/notyouraverage/smscourier/integration/PairingFlowIntegrationTest.kt</files>
  <action>
Create PairingFlowIntegrationTest.kt extending IntegrationTestBase with @IntegrationTest annotation.

Structure with @Nested inner classes for organization:
- `inner class PairRequestFlow` - tests for receiving PAIR_REQUEST
- `inner class PairApprovalFlow` - tests for approving pairing
- `inner class PairRejectionFlow` - tests for rejecting pairing

Test cases to implement (Given-When-Then naming):

**PairRequestFlow:**
1. `givenUnknownDevice_whenReceivePairRequest_thenCreatesPendingReceivedDevice`
   - No device exists, receive "SMSC PAIR_REQUEST" from +1234567890
   - Verify: device created with role=TARGET, status=PENDING_RECEIVED

2. `givenPendingSentDevice_whenReceivePairRequest_thenUpdatesToPendingBoth`
   - setupPendingSent(+1234567890), receive PAIR_REQUEST from same number
   - Verify: device has status reflecting both directions pending (or creates second device)

**PairApprovalFlow:**
3. `givenPendingReceivedDevice_whenApprovePairing_thenSendsPairApprovedAndUpdatesStatus`
   - setupPendingPairingRequest(+1234567890)
   - Call commandHandler.approvePairing(+1234567890, "password123")
   - Verify: device status=APPROVED, passwordHash set, PAIR_APPROVED SMS sent

4. `givenPendingSentDevice_whenReceivePairApproved_thenUpdatesStatusToApproved`
   - setupPendingSent(+1234567890)
   - Process "SMSC PAIR_APPROVED" from +1234567890
   - Verify: device status=APPROVED

**PairRejectionFlow:**
5. `givenPendingReceivedDevice_whenRejectPairing_thenSendsPairRejectedAndRemovesDevice`
   - setupPendingPairingRequest(+1234567890)
   - Call commandHandler.rejectPairing(+1234567890)
   - Verify: PAIR_REJECTED SMS sent, device removed from database

6. `givenPendingSentDevice_whenReceivePairRejected_thenRemovesDevice`
   - setupPendingSent(+1234567890)
   - Process "SMSC PAIR_REJECTED" from +1234567890
   - Verify: device removed from database

Use waitForAsync() after async operations. Use capturingSmsSender.assertSent() for SMS verification.
  </action>
  <verify>
`./gradlew test --tests "*PairingFlowIntegrationTest*"` passes with 6+ tests
  </verify>
  <done>
6 tests cover pairing request, approval, and rejection flows with database and SMS verification
  </done>
</task>

<task type="auto">
  <name>Task 2: Add unpair flow and edge case tests</name>
  <files>app/src/test/java/dev/notyouraverage/smscourier/integration/PairingFlowIntegrationTest.kt</files>
  <action>
Add additional @Nested inner classes to PairingFlowIntegrationTest:
- `inner class UnpairFlow` - tests for UNPAIR command variants
- `inner class EdgeCases` - tests for edge cases and invalid states

Test cases to implement:

**UnpairFlow:**
7. `givenApprovedSourceDevice_whenReceiveUnpairSource_thenRemovesSourceDevice`
   - setupApprovedSourceDevice(+1234567890)
   - Process "SMSC UNPAIR SOURCE" from +1234567890
   - Verify: SOURCE device removed, no SMS sent back

8. `givenApprovedTargetDevice_whenReceiveUnpairTarget_thenRemovesTargetDevice`
   - setupApprovedTargetDevice(+1234567890)
   - Process "SMSC UNPAIR TARGET" from +1234567890
   - Verify: TARGET device removed

9. `givenBidirectionalPairing_whenReceiveUnpairSource_thenRemovesOnlySourceDevice`
   - setupBidirectionalPairing(+1234567890)
   - Process "SMSC UNPAIR SOURCE" from +1234567890
   - Verify: SOURCE removed, TARGET still exists

10. `givenBidirectionalPairing_whenReceiveUnpairWithoutRole_thenRemovesBothDevices`
    - setupBidirectionalPairing(+1234567890)
    - Process "SMSC UNPAIR" (no role) from +1234567890
    - Verify: both SOURCE and TARGET devices removed

**EdgeCases:**
11. `givenAlreadyApprovedDevice_whenReceivePairRequest_thenIgnoresOrRejectsRequest`
    - setupApprovedTargetDevice(+1234567890)
    - Process "SMSC PAIR_REQUEST" from +1234567890
    - Verify: no state change, possibly PAIR_REJECTED sent (check actual behavior)

12. `givenNoDevice_whenReceiveUnpair_thenIgnoresCommand`
    - No device exists
    - Process "SMSC UNPAIR SOURCE" from +1234567890
    - Verify: no crash, no state change, no SMS sent

13. `givenPendingReceivedDevice_whenReceivePairRequestAgain_thenNoStateChange`
    - setupPendingPairingRequest(+1234567890)
    - Process "SMSC PAIR_REQUEST" from +1234567890
    - Verify: device still PENDING_RECEIVED, no duplicate created

Use CommandParser.parse() to create ParsedCommand objects and pass to commandHandler.handleCommand() where needed.
  </action>
  <verify>
`./gradlew test --tests "*PairingFlowIntegrationTest*"` passes with 13+ tests
  </verify>
  <done>
13 tests comprehensively cover all pairing, unpair, and edge case scenarios
  </done>
</task>

</tasks>

<verification>
- [ ] All tests use @IntegrationTest annotation on class
- [ ] Tests use Given-When-Then naming convention
- [ ] Tests use ScenarioBuilders for setup where appropriate
- [ ] Tests verify both database state and SMS output
- [ ] Tests use waitForAsync() for async operations
- [ ] `./gradlew spotlessApply` passes
- [ ] `./gradlew test --tests "*PairingFlowIntegrationTest*"` passes
- [ ] No regressions in full test suite: `./gradlew test` passes
</verification>

<success_criteria>
- PairingFlowIntegrationTest.kt created with 13+ tests
- Tests organized with @Nested inner classes
- All pairing scenarios covered: request, approve, reject, unpair (with role variants), bidirectional, edge cases
- Tests verify actual behavior through real commandHandler execution
</success_criteria>

<output>
After completion, create `.planning/phases/11-e2e-flow-tests/11-01-SUMMARY.md`
</output>
