---
phase: 11-e2e-flow-tests
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/test/java/dev/notyouraverage/smscourier/integration/SecurityIntegrationTest.kt
autonomous: true

must_haves:
  truths:
    - "Failed auth attempts increment failedAttempts counter"
    - "Device locked after MAX_FAILED_ATTEMPTS"
    - "Locked device rejects auth attempts until lockout expires"
    - "Commands from unknown devices are rejected"
    - "Duplicate commands are handled idempotently"
    - "Invalid state transitions are handled gracefully"
  artifacts:
    - path: "app/src/test/java/dev/notyouraverage/smscourier/integration/SecurityIntegrationTest.kt"
      provides: "Security and error handling integration tests"
      min_lines: 200
  key_links:
    - from: "SecurityIntegrationTest"
      to: "SecurityManager.isDeviceLocked"
      via: "securityManager.isDeviceLocked()"
      pattern: "securityManager\\.isDeviceLocked"
    - from: "SecurityIntegrationTest"
      to: "deviceRepository"
      via: "deviceRepository.getByPhoneNumberAndRole()"
      pattern: "deviceRepository\\.getByPhoneNumberAndRole"
---

<objective>
Create integration tests for security scenarios (lockout, failed auth) and error handling (unknown devices, malformed commands, idempotency).

Purpose: Validate security mechanisms and graceful error handling work correctly in real flows.
Output: SecurityIntegrationTest.kt with comprehensive security and error handling coverage.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-e2e-flow-tests/11-CONTEXT.md
@.planning/phases/11-e2e-flow-tests/11-RESEARCH.md

Infrastructure files:
@app/src/test/java/dev/notyouraverage/smscourier/integration/IntegrationTestBase.kt
@app/src/test/java/dev/notyouraverage/smscourier/integration/ScenarioBuilders.kt
@app/src/test/java/dev/notyouraverage/smscourier/integration/CapturingSmsSender.kt

Security implementation:
@app/src/main/java/dev/notyouraverage/smscourier/security/SecurityManager.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SecurityIntegrationTest with lockout and failed auth tests</name>
  <files>app/src/test/java/dev/notyouraverage/smscourier/integration/SecurityIntegrationTest.kt</files>
  <action>
Create SecurityIntegrationTest.kt extending IntegrationTestBase with:

CRITICAL: Do NOT use @Nested classes. JUnit 4 does not support them. Use comment-based grouping instead.

Class structure:
```kotlin
@IntegrationTest
@RunWith(RobolectricTestRunner::class)
@Config(sdk = [34])
@ExperimentalCoroutinesApi
class SecurityIntegrationTest : IntegrationTestBase() {
    // Tests grouped by comment sections
}
```

Tests to implement:

// ==================== Failed Authentication Tests ====================

1. `givenApprovedDevice_whenInvalidHmacResponse_thenFailedAttemptIncremented`
   - Setup: setupApprovedSourceDevice("+1234567890", "password123")
   - Step 1: commandHandler.handleAuthRequest("+1234567890")
   - waitForAsync()
   - capturingSmsSender.clear()
   - Step 2: commandHandler.handleStartForward("+1234567890", "wrong-hmac", 30)
   - waitForAsync()
   - Verify: Device failedAttempts > 0 (retrieve device and check)
   - Verify: No session created

2. `givenApprovedDevice_whenMultipleFailedAttempts_thenCountAccumulates`
   - Setup: setupApprovedSourceDevice("+1234567890", "password123")
   - Loop 3 times:
     - handleAuthRequest, waitForAsync, clear sender
     - handleStartForward with "wrong-hmac", waitForAsync
   - Verify: Device failedAttempts == 3 (or check accumulated count)

// ==================== Lockout Tests ====================

3. `givenLockedDevice_whenAuthRequestReceived_thenLockoutRespected`
   - Setup: setupLockedDevice("+1234567890", "password123", lockDurationMs = 5 * 60 * 1000)
   - Call commandHandler.handleAuthRequest("+1234567890")
   - waitForAsync()
   - Verify: securityManager.isDeviceLocked(device) is true
   - Verify: No AUTH_CHALLENGE sent (or limited response)

4. `givenLockedDevice_whenStartForwardReceived_thenNoSessionCreated`
   - Setup: setupLockedDevice("+1234567890", "password123")
   - Call commandHandler.handleStartForward("+1234567890", "any-response", 30)
   - waitForAsync()
   - Verify: sessionRepository.getActiveSessionsList() is empty

5. `givenDeviceAtMaxFailedAttempts_whenAnotherFailedAttempt_thenLockedOut`
   - Setup: setupApprovedSourceDevice("+1234567890", "password123")
   - Manually set failedAttempts to MAX_FAILED_ATTEMPTS - 1 (or close)
   - Step 1: handleAuthRequest, waitForAsync
   - Step 2: handleStartForward with wrong response
   - waitForAsync()
   - Verify: Device is now locked (lockedUntil set to future)

// ==================== Unknown Device Tests ====================

6. `givenUnknownDevice_whenStartForwardReceived_thenNoSessionCreated`
   - No setup (unknown device)
   - Call commandHandler.handleStartForward("+9999999999", "any-response", 30)
   - waitForAsync()
   - Verify: sessionRepository.getActiveSessionsList() is empty
   - Verify: No error thrown

7. `givenUnknownDevice_whenStopForwardReceived_thenNoError`
   - No setup
   - Call commandHandler.handleStopForward("+9999999999")
   - waitForAsync()
   - Verify: No exception, graceful handling

8. `givenUnknownDevice_whenUnpairReceived_thenNoError`
   - No setup
   - Call commandHandler.handleUnpair("+9999999999", DeviceRole.SOURCE)
   - waitForAsync()
   - Verify: No exception, no SMS sent

Use SecurityManager.MAX_FAILED_ATTEMPTS constant for lockout threshold.
  </action>
  <verify>./gradlew test --tests "*SecurityIntegrationTest*" passes</verify>
  <done>8 security tests passing covering failed auth, lockout, and unknown device scenarios</done>
</task>

<task type="auto">
  <name>Task 2: Add idempotency and edge case tests</name>
  <files>app/src/test/java/dev/notyouraverage/smscourier/integration/SecurityIntegrationTest.kt</files>
  <action>
Add additional tests to SecurityIntegrationTest.kt:

// ==================== Idempotency Tests ====================

9. `givenApprovedDevice_whenDuplicatePairRequestReceived_thenIdempotent`
   - Setup: setupApprovedTargetDevice("+1234567890")
   - Call commandHandler.handlePairRequest("+1234567890") twice
   - waitForAsync() after each
   - Verify: Still one TARGET device with APPROVED status
   - Verify: No duplicate entries created

10. `givenActiveSession_whenDuplicateStopForwardReceived_thenIdempotent`
    - Setup: setupActiveForwardingSession("+1234567890", 30)
    - Call commandHandler.handleStopForward("+1234567890") twice
    - waitForAsync() after each
    - Verify: Session ended once, no error on second call

11. `givenPendingReceived_whenDuplicateApprovalReceived_thenIdempotent`
    - Setup: setupPendingPairingRequest("+1234567890")
    - Call commandHandler.approvePairing("+1234567890", "password123") twice
    - waitForAsync() after each
    - Verify: Device approved, second approval is no-op or idempotent

// ==================== Invalid State Transition Tests ====================

12. `givenNoDevice_whenApprovalAttempted_thenNoError`
    - No setup (no pending device)
    - Call commandHandler.approvePairing("+9999999999", "password")
    - waitForAsync()
    - Verify: No exception, graceful handling

13. `givenApprovedDevice_whenApprovalAttempted_thenNoStateChange`
    - Setup: setupApprovedTargetDevice("+1234567890", "originalPassword")
    - Call commandHandler.approvePairing("+1234567890", "newPassword")
    - waitForAsync()
    - Verify: Status still APPROVED, password unchanged (or verify actual behavior)

14. `givenRejectedDevice_whenApprovalAttempted_thenNoStateChange`
    - Setup: Create rejected device
    - Call commandHandler.approvePairing("+1234567890", "password")
    - waitForAsync()
    - Verify: Status still REJECTED (or verify actual behavior)

// ==================== Rapid Command Tests ====================

15. `givenApprovedDevice_whenRapidAuthRequests_thenLastChallengeValid`
    - Setup: setupApprovedSourceDevice("+1234567890", "password123")
    - Call handleAuthRequest 3 times rapidly (without waiting between)
    - waitForAsync() once at end
    - Extract nonce from last AUTH_CHALLENGE
    - Compute HMAC with last nonce
    - Call handleStartForward with that HMAC
    - waitForAsync()
    - Verify: Session created (last challenge is valid)

Note: For rapid command tests, the goal is to verify the system handles rapid inputs gracefully,
not to test true concurrency (which would require instrumented tests).
  </action>
  <verify>./gradlew test --tests "*SecurityIntegrationTest*" passes (all 15 tests)</verify>
  <done>15 security and edge case tests passing, covering lockout, idempotency, invalid states, and rapid commands</done>
</task>

</tasks>

<verification>
- All tests pass: `./gradlew test --tests "*SecurityIntegrationTest*"`
- Full integration test suite: `./gradlew test --tests "*Integration*"`
- Test file exists and has 200+ lines
- No @Nested annotations present (JUnit 4 compatible)
- Tests verify lockout mechanism works correctly
- Tests verify graceful handling of edge cases
</verification>

<success_criteria>
- SecurityIntegrationTest.kt exists with 15+ tests
- Tests cover: failed auth, lockout, unknown devices, idempotency, invalid state transitions
- All tests pass
- Tests follow Given-When-Then naming convention
- Tests use comment-based grouping (not @Nested)
</success_criteria>

<output>
After completion, create `.planning/phases/11-e2e-flow-tests/11-03-SUMMARY.md`
</output>
