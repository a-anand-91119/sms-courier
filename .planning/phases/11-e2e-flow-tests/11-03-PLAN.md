---
phase: 11-e2e-flow-tests
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/test/java/dev/notyouraverage/smscourier/integration/SecurityAndErrorIntegrationTest.kt
autonomous: true

must_haves:
  truths:
    - "Repeated auth failures lock the device"
    - "Locked device rejects all auth attempts"
    - "Commands from unknown (unpaired) devices are rejected"
    - "Malformed SMSC commands do not crash and are ignored"
    - "Duplicate commands are handled gracefully (idempotency)"
    - "Invalid state transitions are rejected"
  artifacts:
    - path: "app/src/test/java/dev/notyouraverage/smscourier/integration/SecurityAndErrorIntegrationTest.kt"
      provides: "Security and error handling integration tests"
      min_lines: 200
      contains: "@IntegrationTest"
  key_links:
    - from: "SecurityAndErrorIntegrationTest"
      to: "securityManager"
      via: "lockout verification"
      pattern: "securityManager\\.(isDeviceLocked|recordFailedAttempt)"
    - from: "SecurityAndErrorIntegrationTest"
      to: "commandHandler"
      via: "error handling paths"
      pattern: "commandHandler\\.handle"
---

<objective>
Create integration tests for security flows and error handling scenarios.

Purpose: Verify authentication lockout, command rejection from unknown devices, malformed command handling, and idempotency.
Output: SecurityAndErrorIntegrationTest.kt with comprehensive security and error scenario coverage.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-e2e-flow-tests/11-CONTEXT.md

# Phase 10 infrastructure
@app/src/test/java/dev/notyouraverage/smscourier/integration/IntegrationTestBase.kt
@app/src/test/java/dev/notyouraverage/smscourier/integration/ScenarioBuilders.kt
@app/src/test/java/dev/notyouraverage/smscourier/integration/CapturingSmsSender.kt

# Source code being tested
@app/src/main/java/dev/notyouraverage/smscourier/handlers/SmsCommandHandler.kt
@app/src/main/java/dev/notyouraverage/smscourier/security/SecurityManager.kt
@app/src/main/java/dev/notyouraverage/smscourier/commands/CommandParser.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SecurityAndErrorIntegrationTest with lockout and unknown device tests</name>
  <files>app/src/test/java/dev/notyouraverage/smscourier/integration/SecurityAndErrorIntegrationTest.kt</files>
  <action>
Create SecurityAndErrorIntegrationTest.kt extending IntegrationTestBase with @IntegrationTest annotation.

Structure with @Nested inner classes:
- `inner class AuthLockoutFlow` - tests for failed auth attempts leading to lockout
- `inner class UnknownDeviceRejection` - tests for commands from unpaired devices

Test cases to implement:

**AuthLockoutFlow:**
1. `givenApprovedDevice_whenMultipleFailedAuthAttempts_thenDeviceBecomesLocked`
   - setupApprovedTargetDevice(+1234567890, "password123")
   - Repeatedly send AUTH_REQUEST and then invalid START_FORWARD (wrong response)
   - Do this SecurityManager.MAX_FAILED_ATTEMPTS times (typically 5)
   - Verify: securityManager.isDeviceLocked(device) returns true, device.failedAttempts >= MAX

2. `givenLockedDevice_whenReceiveAuthRequest_thenNoChallengeSent`
   - setupLockedDevice(+1234567890, "password123")
   - Process "SMSC AUTH_REQUEST" from +1234567890
   - Verify: no AUTH_CHALLENGE sent (assertNothingSent or no AUTH_CHALLENGE in messages)

3. `givenLockedDevice_whenReceiveStartForward_thenRejected`
   - setupLockedDevice(+1234567890, "password123")
   - Process "SMSC START_FORWARD someresponse" from +1234567890
   - Verify: no session created, lockout persists

4. `givenLockedDevice_whenLockoutExpires_thenAuthRequestWorks`
   - Create device with lockedUntil in the past (lockout expired)
   - Process "SMSC AUTH_REQUEST" from +1234567890
   - Verify: AUTH_CHALLENGE sent, lockout cleared

**UnknownDeviceRejection:**
5. `givenNoDevice_whenReceiveStartForward_thenIgnored`
   - No device in database
   - Process "SMSC START_FORWARD password123" from +9999999999
   - Verify: no session created, no crash, assertNothingSent

6. `givenNoDevice_whenReceiveStopForward_thenIgnored`
   - No device in database
   - Process "SMSC STOP_FORWARD" from +9999999999
   - Verify: no error, no crash

7. `givenApprovedDevice_whenReceiveCommandFromDifferentPhone_thenIgnored`
   - setupApprovedTargetDevice(+1234567890, "password123")
   - Process "SMSC AUTH_REQUEST" from +9999999999 (different phone)
   - Verify: no AUTH_CHALLENGE sent to +9999999999

Check SecurityManager for MAX_FAILED_ATTEMPTS constant value. Use setupLockedDevice with negative lockDurationMs to simulate expired lockout.
  </action>
  <verify>
`./gradlew test --tests "*SecurityAndErrorIntegrationTest*"` passes with 7+ tests
  </verify>
  <done>
7 tests cover authentication lockout flow and unknown device rejection
  </done>
</task>

<task type="auto">
  <name>Task 2: Add malformed command and idempotency tests</name>
  <files>app/src/test/java/dev/notyouraverage/smscourier/integration/SecurityAndErrorIntegrationTest.kt</files>
  <action>
Add additional @Nested inner classes to SecurityAndErrorIntegrationTest:
- `inner class MalformedCommandHandling` - tests for invalid/malformed SMSC commands
- `inner class IdempotencyAndRaceConditions` - tests for duplicate command handling
- `inner class InvalidStateTransitions` - tests for commands in wrong state

Test cases to implement:

**MalformedCommandHandling:**
8. `givenApprovedDevice_whenReceiveMalformedSmscCommand_thenIgnoredNoError`
   - setupApprovedTargetDevice(+1234567890)
   - Test various malformed commands:
     - "SMSC" (no command type)
     - "SMSC INVALID_COMMAND"
     - "SMSC START_FORWARD" (missing response)
     - "SMSC FWD" (missing sender and message)
   - Verify: no crash, no state change, no SMS sent

9. `givenAnyState_whenReceivePartialSmscPrefix_thenIgnored`
   - Test messages that start with "SMS" but not "SMSC"
   - Test "SMSC" followed by garbage
   - Verify: treated as regular SMS (not command), no crash

10. `givenAnyState_whenReceiveNonSmscMessage_thenNotTreatedAsCommand`
    - Regular SMS "Hello world" should not trigger command handling
    - Verify: if active session, forwards as regular SMS; if no session, ignored

**IdempotencyAndRaceConditions:**
11. `givenPendingReceivedDevice_whenReceiveDuplicatePairRequest_thenNoDuplicateDevice`
    - setupPendingPairingRequest(+1234567890)
    - Process "SMSC PAIR_REQUEST" again from same number
    - Verify: still only one device in database, status unchanged

12. `givenActiveSession_whenReceiveMultipleStopForward_thenNoError`
    - setupActiveForwardingSession(+1234567890)
    - Process "SMSC STOP_FORWARD" twice in rapid succession
    - Verify: session stopped, second stop is no-op, no error

13. `givenActiveSession_whenReceiveStartForwardAgain_thenExistingSessionContinues`
    - Start a session successfully (full auth flow)
    - Attempt to start another session without stopping first
    - Verify: behavior defined (either replaces session or rejects)

**InvalidStateTransitions:**
14. `givenPendingReceivedDevice_whenReceiveStartForward_thenRejected`
    - setupPendingPairingRequest(+1234567890) (not approved yet)
    - Process "SMSC START_FORWARD response" from +1234567890
    - Verify: no session created (pairing not complete)

15. `givenApprovedSourceDevice_whenReceiveAuthRequest_thenNoResponse`
    - setupApprovedSourceDevice(+1234567890) (role is SOURCE, not TARGET)
    - Process "SMSC AUTH_REQUEST" from +1234567890
    - Verify: no AUTH_CHALLENGE (only TARGET devices issue challenges)

Test malformed commands by calling commandHandler.handleCommand() with ParsedCommand objects or by simulating the full flow through the command parser.
  </action>
  <verify>
`./gradlew test --tests "*SecurityAndErrorIntegrationTest*"` passes with 15+ tests
  </verify>
  <done>
15 tests comprehensively cover security lockout, unknown device rejection, malformed commands, idempotency, and invalid state transitions
  </done>
</task>

</tasks>

<verification>
- [ ] All tests use @IntegrationTest annotation on class
- [ ] Tests use Given-When-Then naming convention
- [ ] Lockout tests verify SecurityManager.MAX_FAILED_ATTEMPTS threshold
- [ ] Malformed command tests do not cause exceptions
- [ ] Idempotency tests verify no duplicate state
- [ ] `./gradlew spotlessApply` passes
- [ ] `./gradlew test --tests "*SecurityAndErrorIntegrationTest*"` passes
- [ ] No regressions: `./gradlew test` passes
</verification>

<success_criteria>
- SecurityAndErrorIntegrationTest.kt created with 15+ tests
- Tests organized with @Nested inner classes
- Security scenarios covered: lockout flow, lockout expiry, unknown device rejection
- Error handling covered: malformed commands, partial commands, non-SMSC messages
- Edge cases covered: idempotency, race conditions, invalid state transitions
</success_criteria>

<output>
After completion, create `.planning/phases/11-e2e-flow-tests/11-03-SUMMARY.md`
</output>
