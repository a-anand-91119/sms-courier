---
phase: 11-e2e-flow-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/test/java/dev/notyouraverage/smscourier/integration/ForwardingFlowIntegrationTest.kt
autonomous: true

must_haves:
  truths:
    - "Auth request generates challenge and sends AUTH_CHALLENGE SMS"
    - "Valid HMAC response starts forwarding session"
    - "Messages forwarded to all active sessions"
    - "Stop forward ends session and marks stoppedBy"
    - "Session tracks message count correctly"
    - "Multi-device forwarding sends to all active sessions"
  artifacts:
    - path: "app/src/test/java/dev/notyouraverage/smscourier/integration/ForwardingFlowIntegrationTest.kt"
      provides: "Forwarding flow integration tests"
      min_lines: 250
  key_links:
    - from: "ForwardingFlowIntegrationTest"
      to: "SmsCommandHandler.handleAuthRequest"
      via: "commandHandler.handleAuthRequest()"
      pattern: "commandHandler\\.handleAuthRequest"
    - from: "ForwardingFlowIntegrationTest"
      to: "SmsCommandHandler.handleStartForward"
      via: "commandHandler.handleStartForward()"
      pattern: "commandHandler\\.handleStartForward"
    - from: "ForwardingFlowIntegrationTest"
      to: "SmsCommandHandler.handleIncomingSms"
      via: "commandHandler.handleIncomingSms()"
      pattern: "commandHandler\\.handleIncomingSms"
---

<objective>
Create integration tests for the complete forwarding flow covering authentication, session management, and message forwarding.

Purpose: Validate that the challenge-response authentication and SMS forwarding work end-to-end with correct session lifecycle.
Output: ForwardingFlowIntegrationTest.kt with comprehensive forwarding flow coverage.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-e2e-flow-tests/11-CONTEXT.md
@.planning/phases/11-e2e-flow-tests/11-RESEARCH.md

Infrastructure files:
@app/src/test/java/dev/notyouraverage/smscourier/integration/IntegrationTestBase.kt
@app/src/test/java/dev/notyouraverage/smscourier/integration/ScenarioBuilders.kt
@app/src/test/java/dev/notyouraverage/smscourier/integration/CapturingSmsSender.kt

Handler implementation:
@app/src/main/java/dev/notyouraverage/smscourier/handlers/SmsCommandHandler.kt
@app/src/main/java/dev/notyouraverage/smscourier/security/SecurityManager.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ForwardingFlowIntegrationTest with auth challenge and session tests</name>
  <files>app/src/test/java/dev/notyouraverage/smscourier/integration/ForwardingFlowIntegrationTest.kt</files>
  <action>
Create ForwardingFlowIntegrationTest.kt extending IntegrationTestBase with:

CRITICAL: Do NOT use @Nested classes. JUnit 4 does not support them. Use comment-based grouping instead.

Class structure:
```kotlin
@IntegrationTest
@RunWith(RobolectricTestRunner::class)
@Config(sdk = [34])
@ExperimentalCoroutinesApi
class ForwardingFlowIntegrationTest : IntegrationTestBase() {
    // Tests grouped by comment sections
}
```

Tests to implement:

// ==================== Authentication Challenge Tests ====================

1. `givenApprovedSourceDevice_whenAuthRequestReceived_thenChallengeSent`
   - Setup: setupApprovedSourceDevice("+1234567890", "password123")
   - Call commandHandler.handleAuthRequest("+1234567890")
   - waitForAsync()
   - Verify: capturingSmsSender has message matching Regex("SMSC AUTH_CHALLENGE .+")
   - Verify: securityManager.hasPendingChallenge("+1234567890") is true

2. `givenApprovedSourceDevice_whenAuthRequestReceived_thenNonceIsBase64`
   - Setup: setupApprovedSourceDevice("+1234567890", "password123")
   - Call commandHandler.handleAuthRequest("+1234567890")
   - waitForAsync()
   - Extract nonce from "SMSC AUTH_CHALLENGE {nonce}"
   - Verify: nonce is valid Base64 (can decode without exception)

3. `givenNoDevice_whenAuthRequestReceived_thenNoChallengeGenerated`
   - No setup (unknown device)
   - Call commandHandler.handleAuthRequest("+9999999999")
   - waitForAsync()
   - Verify: No AUTH_CHALLENGE message sent

// ==================== Session Start Tests ====================

4. `givenApprovedSourceDevice_whenValidHmacResponse_thenSessionCreated`
   - Setup: setupApprovedSourceDevice("+1234567890", "password123")
   - Step 1: commandHandler.handleAuthRequest("+1234567890")
   - waitForAsync()
   - Extract nonce from AUTH_CHALLENGE message
   - Compute correct HMAC: SecurityManager.deriveAuthKey("password123") -> computeHmac(authKey, nonce)
   - capturingSmsSender.clear()
   - Step 2: commandHandler.handleStartForward("+1234567890", hmacResponse, 30)
   - waitForAsync()
   - Verify: sessionRepository.getActiveSessionsList() has 1 session
   - Verify: Session devicePhoneNumber is "+1234567890"
   - Verify: Session durationMinutes is 30
   - Verify: Session isActive is true

5. `givenApprovedSourceDevice_whenInvalidHmacResponse_thenNoSessionCreated`
   - Setup: setupApprovedSourceDevice("+1234567890", "password123")
   - Step 1: commandHandler.handleAuthRequest("+1234567890")
   - waitForAsync()
   - capturingSmsSender.clear()
   - Step 2: commandHandler.handleStartForward("+1234567890", "invalid-response", 30)
   - waitForAsync()
   - Verify: sessionRepository.getActiveSessionsList() is empty
   - Verify: Failed attempt tracked (check device.failedAttempts if accessible)

6. `givenNoChallenge_whenStartForwardReceived_thenNoSessionCreated`
   - Setup: setupApprovedSourceDevice("+1234567890", "password123")
   - No AUTH_REQUEST step (skip challenge generation)
   - Call commandHandler.handleStartForward("+1234567890", "any-response", 30)
   - waitForAsync()
   - Verify: sessionRepository.getActiveSessionsList() is empty

Required imports:
- dev.notyouraverage.smscourier.security.SecurityManager
- Use SecurityManager.deriveAuthKey() and SecurityManager.computeHmac()
  </action>
  <verify>./gradlew test --tests "*ForwardingFlowIntegrationTest*" passes</verify>
  <done>6 authentication and session start tests passing</done>
</task>

<task type="auto">
  <name>Task 2: Add message forwarding and session stop tests</name>
  <files>app/src/test/java/dev/notyouraverage/smscourier/integration/ForwardingFlowIntegrationTest.kt</files>
  <action>
Add additional tests to ForwardingFlowIntegrationTest.kt:

// ==================== Message Forwarding Tests ====================

7. `givenActiveSession_whenIncomingSmsReceived_thenMessageForwarded`
   - Setup: setupActiveForwardingSession("+1234567890", 30)
   - Call commandHandler.handleIncomingSms("+5555555555", "Your OTP is 123456")
   - waitForAsync()
   - Verify: capturingSmsSender has message to "+1234567890"
   - Verify: Message contains "SMSC FWD" or "SMSC FWDE" prefix
   - Verify: Message contains original sender "+5555555555" or encrypted content

8. `givenActiveSession_whenMultipleMessagesReceived_thenMessageCountIncremented`
   - Setup: setupActiveForwardingSession("+1234567890", 30)
   - Call commandHandler.handleIncomingSms("+5555555555", "Message 1")
   - waitForAsync()
   - Call commandHandler.handleIncomingSms("+6666666666", "Message 2")
   - waitForAsync()
   - Verify: Session messagesForwarded == 2
   - Verify: capturingSmsSender.sentMessages.size == 2

9. `givenNoActiveSession_whenIncomingSmsReceived_thenNoForwarding`
   - Setup: setupApprovedSourceDevice("+1234567890") (no active session)
   - Call commandHandler.handleIncomingSms("+5555555555", "Test message")
   - waitForAsync()
   - Verify: capturingSmsSender.assertNothingSent()

10. `givenMultipleActiveSessions_whenIncomingSmsReceived_thenForwardedToAll`
    - Setup: setupActiveForwardingSession("+1111111111", 30)
    - Setup: setupActiveForwardingSession("+2222222222", 30)
    - Call commandHandler.handleIncomingSms("+5555555555", "Broadcast message")
    - waitForAsync()
    - Verify: capturingSmsSender.sentMessages.size == 2
    - Verify: Messages sent to both "+1111111111" and "+2222222222"

// ==================== Session Stop Tests ====================

11. `givenActiveSession_whenStopForwardReceived_thenSessionEnded`
    - Setup: setupActiveForwardingSession("+1234567890", 30)
    - Verify: Session is active before stop
    - Call commandHandler.handleStopForward("+1234567890")
    - waitForAsync()
    - Verify: Session isActive is false
    - Verify: Session stoppedBy is "REMOTE" (or check actual value)

12. `givenNoActiveSession_whenStopForwardReceived_thenNoError`
    - Setup: setupApprovedSourceDevice("+1234567890") (no session)
    - Call commandHandler.handleStopForward("+1234567890")
    - waitForAsync()
    - Verify: No exception, graceful handling

// ==================== Full Forwarding Flow Tests ====================

13. `fullForwardingFlow_authThenForwardThenStop`
    - Setup: setupApprovedSourceDevice("+1234567890", "password123")
    - Step 1: AUTH_REQUEST
    - Extract nonce, compute HMAC
    - capturingSmsSender.clear()
    - Step 2: START_FORWARD with valid response
    - Checkpoint: Session active
    - Step 3: Receive incoming SMS
    - Checkpoint: Message forwarded, count incremented
    - Step 4: STOP_FORWARD
    - Verify: Session ended, stoppedBy set
  </action>
  <verify>./gradlew test --tests "*ForwardingFlowIntegrationTest*" passes (all 13 tests)</verify>
  <done>13 forwarding flow tests passing, covering auth, forwarding, multi-device, and session stop</done>
</task>

</tasks>

<verification>
- All tests pass: `./gradlew test --tests "*ForwardingFlowIntegrationTest*"`
- Full integration test suite: `./gradlew test --tests "*Integration*"`
- Test file exists and has 250+ lines
- No @Nested annotations present (JUnit 4 compatible)
- Tests properly compute HMAC using SecurityManager methods
- Tests verify session lifecycle (create, forward, stop)
</verification>

<success_criteria>
- ForwardingFlowIntegrationTest.kt exists with 13+ tests
- Tests cover: auth challenge, session start, message forwarding, multi-device, session stop
- All tests pass
- Tests follow Given-When-Then naming convention
- Tests use comment-based grouping (not @Nested)
</success_criteria>

<output>
After completion, create `.planning/phases/11-e2e-flow-tests/11-02-SUMMARY.md`
</output>
