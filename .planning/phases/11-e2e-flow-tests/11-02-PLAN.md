---
phase: 11-e2e-flow-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/test/java/dev/notyouraverage/smscourier/integration/ForwardingFlowIntegrationTest.kt
autonomous: true

must_haves:
  truths:
    - "AUTH_REQUEST from paired device triggers AUTH_CHALLENGE response"
    - "Valid START_FORWARD with correct challenge response starts session"
    - "Active session forwards incoming SMS to paired device"
    - "STOP_FORWARD ends active session"
    - "Invalid auth response does not start session and increments failed attempts"
    - "Session expiry stops forwarding"
  artifacts:
    - path: "app/src/test/java/dev/notyouraverage/smscourier/integration/ForwardingFlowIntegrationTest.kt"
      provides: "Complete forwarding flow integration tests"
      min_lines: 250
      contains: "@IntegrationTest"
  key_links:
    - from: "ForwardingFlowIntegrationTest"
      to: "commandHandler"
      via: "handleAuthRequest/handleStartForward/handleIncomingSms/handleStopForward"
      pattern: "commandHandler\\."
    - from: "ForwardingFlowIntegrationTest"
      to: "sessionRepository"
      via: "session state verification"
      pattern: "sessionRepository\\."
    - from: "ForwardingFlowIntegrationTest"
      to: "securityManager"
      via: "challenge generation and verification"
      pattern: "securityManager\\."
---

<objective>
Create comprehensive integration tests for the complete forwarding flow.

Purpose: Verify the auth -> start -> forward -> stop flow works end-to-end with real challenge-response authentication.
Output: ForwardingFlowIntegrationTest.kt with tests for authentication, session management, and message forwarding.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-e2e-flow-tests/11-CONTEXT.md

# Phase 10 infrastructure
@app/src/test/java/dev/notyouraverage/smscourier/integration/IntegrationTestBase.kt
@app/src/test/java/dev/notyouraverage/smscourier/integration/ScenarioBuilders.kt
@app/src/test/java/dev/notyouraverage/smscourier/integration/CapturingSmsSender.kt

# Source code being tested
@app/src/main/java/dev/notyouraverage/smscourier/handlers/SmsCommandHandler.kt
@app/src/main/java/dev/notyouraverage/smscourier/security/SecurityManager.kt
@app/src/main/java/dev/notyouraverage/smscourier/commands/CommandParser.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ForwardingFlowIntegrationTest with auth and session start tests</name>
  <files>app/src/test/java/dev/notyouraverage/smscourier/integration/ForwardingFlowIntegrationTest.kt</files>
  <action>
Create ForwardingFlowIntegrationTest.kt extending IntegrationTestBase with @IntegrationTest annotation.

Structure with @Nested inner classes:
- `inner class AuthenticationFlow` - AUTH_REQUEST and challenge-response tests
- `inner class SessionStartFlow` - START_FORWARD with valid/invalid auth

Test cases to implement:

**AuthenticationFlow:**
1. `givenApprovedTargetDevice_whenReceiveAuthRequest_thenSendsAuthChallenge`
   - setupApprovedTargetDevice(+1234567890, "password123")
   - Process "SMSC AUTH_REQUEST" from +1234567890
   - Verify: AUTH_CHALLENGE SMS sent with nonce, securityManager.hasPendingChallenge() returns true

2. `givenNoDevice_whenReceiveAuthRequest_thenNoResponse`
   - No device exists
   - Process "SMSC AUTH_REQUEST" from +1234567890
   - Verify: no SMS sent (assertNothingSent)

3. `givenPendingDevice_whenReceiveAuthRequest_thenNoResponse`
   - setupPendingPairingRequest(+1234567890)
   - Process "SMSC AUTH_REQUEST" from +1234567890
   - Verify: no SMS sent (device not approved)

**SessionStartFlow:**
4. `givenPendingChallenge_whenReceiveValidStartForward_thenStartsSession`
   - setupApprovedTargetDevice(+1234567890, "password123")
   - Call commandHandler.handleAuthRequest(+1234567890) to generate challenge
   - Capture the nonce from AUTH_CHALLENGE SMS
   - Compute correct HMAC response using SecurityManager.computeChallengeResponse()
   - Process "SMSC START_FORWARD <response>" from +1234567890
   - Verify: session created in sessionRepository, session.isActive = true

5. `givenPendingChallenge_whenReceiveStartForwardWithDuration_thenStartsSessionWithDuration`
   - Same as above but with "SMSC START_FORWARD <response> 60" (60 minutes)
   - Verify: session.durationMinutes = 60

6. `givenPendingChallenge_whenReceiveInvalidResponse_thenNoSessionAndIncrementFailedAttempts`
   - setupApprovedTargetDevice(+1234567890, "password123")
   - Generate challenge via handleAuthRequest
   - Process "SMSC START_FORWARD wrongresponse" from +1234567890
   - Verify: no session created, device.failedAttempts incremented

7. `givenNoChallenge_whenReceiveStartForward_thenNoSession`
   - setupApprovedTargetDevice(+1234567890, "password123")
   - Process "SMSC START_FORWARD someresponse" (no AUTH_REQUEST first)
   - Verify: no session created

Use capturingSmsSender.findByPrefix("SMSC AUTH_CHALLENGE") to extract nonce.
Parse nonce from message format "SMSC AUTH_CHALLENGE <nonce>".
  </action>
  <verify>
`./gradlew test --tests "*ForwardingFlowIntegrationTest*"` passes with 7+ tests
  </verify>
  <done>
7 tests cover authentication flow and session start with valid/invalid scenarios
  </done>
</task>

<task type="auto">
  <name>Task 2: Add message forwarding and session stop tests</name>
  <files>app/src/test/java/dev/notyouraverage/smscourier/integration/ForwardingFlowIntegrationTest.kt</files>
  <action>
Add additional @Nested inner classes to ForwardingFlowIntegrationTest:
- `inner class MessageForwardingFlow` - tests for SMS forwarding during active session
- `inner class SessionStopFlow` - tests for STOP_FORWARD command
- `inner class SessionExpiryFlow` - tests for session timeout behavior

Test cases to implement:

**MessageForwardingFlow:**
8. `givenActiveSession_whenReceiveRegularSms_thenForwardsToSource`
   - setupActiveForwardingSession(+1234567890) (creates SOURCE device + active session)
   - Call commandHandler.handleIncomingSms("+5555555555", "Your OTP is 123456")
   - Verify: SMS sent to +1234567890 with format "SMSC FWD <+5555555555> Your OTP is 123456"

9. `givenActiveSession_whenReceiveMultipleSms_thenForwardsAllToSource`
   - setupActiveForwardingSession(+1234567890)
   - Send 3 different SMS via handleIncomingSms
   - Verify: 3 forwarded messages captured, session.forwardedCount incremented

10. `givenNoActiveSession_whenReceiveRegularSms_thenNoForwarding`
    - setupApprovedSourceDevice(+1234567890) but no session
    - Call commandHandler.handleIncomingSms("+5555555555", "Test message")
    - Verify: no SMS forwarded (assertNothingSent or only non-FWD messages)

11. `givenMultipleActiveSessions_whenReceiveRegularSms_thenForwardsToAllSources`
    - Setup two SOURCE devices with active sessions (different phone numbers)
    - Call handleIncomingSms once
    - Verify: SMS forwarded to BOTH source devices

**SessionStopFlow:**
12. `givenActiveSession_whenReceiveStopForward_thenEndsSession`
    - setupActiveForwardingSession(+1234567890)
    - Process "SMSC STOP_FORWARD" from +1234567890
    - Verify: session.isActive = false, no more forwarding occurs

13. `givenNoActiveSession_whenReceiveStopForward_thenNoError`
    - setupApprovedSourceDevice(+1234567890) with no session
    - Process "SMSC STOP_FORWARD" from +1234567890
    - Verify: no crash, no error SMS sent

**SessionExpiryFlow:**
14. `givenExpiredSession_whenReceiveRegularSms_thenNoForwarding`
    - Create session with very short duration or manually set expiresAt to past
    - Call handleIncomingSms
    - Verify: no forwarding (session expired)

Note: Session expiry may require direct database manipulation to set expiresAt in the past, as time simulation is complex. Check if sessionRepository allows updating session expiry or create session with past expiresAt.
  </action>
  <verify>
`./gradlew test --tests "*ForwardingFlowIntegrationTest*"` passes with 14+ tests
  </verify>
  <done>
14 tests comprehensively cover authentication, session start, message forwarding, session stop, and expiry scenarios
  </done>
</task>

</tasks>

<verification>
- [ ] All tests use @IntegrationTest annotation on class
- [ ] Tests use Given-When-Then naming convention
- [ ] Tests properly simulate challenge-response authentication flow
- [ ] Tests verify session state in database
- [ ] Tests verify SMS forwarding format matches "SMSC FWD <sender> message"
- [ ] `./gradlew spotlessApply` passes
- [ ] `./gradlew test --tests "*ForwardingFlowIntegrationTest*"` passes
- [ ] No regressions: `./gradlew test` passes
</verification>

<success_criteria>
- ForwardingFlowIntegrationTest.kt created with 14+ tests
- Tests organized with @Nested inner classes
- Full auth flow tested: AUTH_REQUEST -> AUTH_CHALLENGE -> START_FORWARD
- Message forwarding verified with proper FWD format
- Session lifecycle tested: start, forward, stop, expiry
</success_criteria>

<output>
After completion, create `.planning/phases/11-e2e-flow-tests/11-02-SUMMARY.md`
</output>
