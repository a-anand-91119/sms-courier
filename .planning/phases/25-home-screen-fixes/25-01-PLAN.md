---
phase: 25-home-screen-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/dev/notyouraverage/smscourier/data/Direction.kt
  - app/src/main/java/dev/notyouraverage/smscourier/viewmodels/HomeViewModel.kt
  - app/src/main/java/dev/notyouraverage/smscourier/viewmodels/PairedDevicesViewModel.kt
  - app/src/test/java/dev/notyouraverage/smscourier/data/DirectionalStatusTest.kt
autonomous: true

must_haves:
  truths:
    - "TARGET device direction maps to FORWARDING_TO (this device forwards to others)"
    - "SOURCE device direction maps to RECEIVING_FROM (this device receives from others)"
    - "HomeViewModel UAT HOME-01 test passes without test modification"
    - "All existing DirectionalStatusTest tests pass after updating the mirrored helper"
  artifacts:
    - path: "app/src/main/java/dev/notyouraverage/smscourier/data/Direction.kt"
      provides: "Direction enum with corrected doc comments matching new semantic mapping"
      contains: "FORWARDING_TO"
    - path: "app/src/main/java/dev/notyouraverage/smscourier/viewmodels/HomeViewModel.kt"
      provides: "calculateDirectionalStatus with swapped SOURCE/TARGET direction mapping"
    - path: "app/src/main/java/dev/notyouraverage/smscourier/viewmodels/PairedDevicesViewModel.kt"
      provides: "getDirectionForDevice with swapped SOURCE/TARGET direction mapping"
    - path: "app/src/test/java/dev/notyouraverage/smscourier/data/DirectionalStatusTest.kt"
      provides: "Updated test helper and test assertions matching new mapping"
  key_links:
    - from: "HomeViewModel.calculateDirectionalStatus"
      to: "Direction enum"
      via: "SOURCE -> RECEIVING_FROM, TARGET -> FORWARDING_TO mapping"
      pattern: "DeviceRole\\.SOURCE.*RECEIVING_FROM|DeviceRole\\.TARGET.*FORWARDING_TO"
    - from: "PairedDevicesViewModel.getDirectionForDevice"
      to: "Direction enum"
      via: "Same swapped mapping"
      pattern: "DeviceRole\\.SOURCE.*RECEIVING_FROM|DeviceRole\\.TARGET.*FORWARDING_TO"
---

<objective>
Fix the Direction enum semantic mapping so TARGET devices map to FORWARDING_TO and SOURCE devices map to RECEIVING_FROM, matching the user-facing meaning: a TARGET device "forwards to" others, a SOURCE device "receives from" others.

Purpose: This is the core data-layer fix for HOME-01. The Direction enum names are fine but the mapping from DeviceRole to Direction is inverted. Swapping this mapping in HomeViewModel and PairedDevicesViewModel fixes the root cause. The UAT HOME-01 test should pass without any test changes.

Output: Corrected Direction mapping in all ViewModels and test helpers. UAT HOME-01 test passes.
</objective>

<execution_context>
@/Users/aanand/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aanand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/src/main/java/dev/notyouraverage/smscourier/data/Direction.kt
@app/src/main/java/dev/notyouraverage/smscourier/viewmodels/HomeViewModel.kt
@app/src/main/java/dev/notyouraverage/smscourier/viewmodels/PairedDevicesViewModel.kt
@app/src/test/java/dev/notyouraverage/smscourier/data/DirectionalStatusTest.kt
@app/src/test/java/dev/notyouraverage/smscourier/viewmodels/HomeViewModelTest.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap Direction mapping in Direction.kt docs, HomeViewModel, and PairedDevicesViewModel</name>
  <files>
    app/src/main/java/dev/notyouraverage/smscourier/data/Direction.kt
    app/src/main/java/dev/notyouraverage/smscourier/viewmodels/HomeViewModel.kt
    app/src/main/java/dev/notyouraverage/smscourier/viewmodels/PairedDevicesViewModel.kt
  </files>
  <action>
    The Direction enum values FORWARDING_TO and RECEIVING_FROM keep their names but their semantic meaning swaps:

    **Direction.kt** — Update doc comments only (enum value names stay the same):
    - `FORWARDING_TO`: Change doc to "This device is TARGET role — forwarding messages TO source device(s). Messages flow FROM this device (arrow up)."
    - `RECEIVING_FROM`: Change doc to "This device is SOURCE role — receiving messages FROM target device(s). Messages flow TO this device (arrow down)."
    - `BIDIRECTIONAL`: No change needed.
    - Update the class-level doc comment:
      - `FORWARDING_TO`: This device forwards messages to others (TARGET role)
      - `RECEIVING_FROM`: This device receives messages from others (SOURCE role)
    - Update DirectionalStatus doc comments:
      - `forwardingToCount`: Sessions where this device FORWARDS messages (TARGET role)
      - `receivingFromCount`: Sessions where this device RECEIVES messages (SOURCE role)

    **HomeViewModel.kt** — In `calculateDirectionalStatus()`, swap the direction assignments:
    - Where `sourceDevice != null` (SOURCE only): Change `Direction.FORWARDING_TO` to `Direction.RECEIVING_FROM` and change `forwardingToCount` increment to `receivingFromCount` increment
    - Where `targetDevice != null` (TARGET only): Change `Direction.RECEIVING_FROM` to `Direction.FORWARDING_TO` and change `receivingFromCount` increment to `forwardingToCount` increment
    - The BIDIRECTIONAL case stays the same (both roles present)

    **PairedDevicesViewModel.kt** — In `getDirectionForDevice()`, swap the mapping:
    - `DeviceRole.SOURCE` -> `Direction.RECEIVING_FROM` (was FORWARDING_TO)
    - `DeviceRole.TARGET` -> `Direction.FORWARDING_TO` (was RECEIVING_FROM)
    - Update the inline comments to match new semantics

    IMPORTANT: Do NOT rename the enum values themselves. Only swap the mapping from DeviceRole to Direction values.
  </action>
  <verify>
    Run: `./gradlew test --tests "dev.notyouraverage.smscourier.viewmodels.HomeViewModelTest"`
    The UAT HOME-01 test should now PASS (it expects TARGET -> FORWARDING_TO).
    Other HomeViewModelTest tests should continue to pass.
  </verify>
  <done>
    - Direction.kt doc comments match new semantic mapping
    - HomeViewModel maps SOURCE -> RECEIVING_FROM, TARGET -> FORWARDING_TO
    - PairedDevicesViewModel maps SOURCE -> RECEIVING_FROM, TARGET -> FORWARDING_TO
    - HomeViewModelTest UAT HOME-01 test passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Update DirectionalStatusTest helper and assertions to match swapped mapping</name>
  <files>
    app/src/test/java/dev/notyouraverage/smscourier/data/DirectionalStatusTest.kt
  </files>
  <action>
    The DirectionalStatusTest has a private `calculateDirectionalStatus()` helper that mirrors HomeViewModel's logic. It must be updated to match the swapped mapping.

    In the test helper function `calculateDirectionalStatus()`:
    - Where `sourceDevice != null` only: Change `Direction.FORWARDING_TO` to `Direction.RECEIVING_FROM`, change `forwardingToCount` increment to `receivingFromCount` increment
    - Where `targetDevice != null` only: Change `Direction.RECEIVING_FROM` to `Direction.FORWARDING_TO`, change `receivingFromCount` increment to `forwardingToCount` increment
    - BIDIRECTIONAL case: No change

    Then update test assertions that reference the old mapping:

    1. `only SOURCE sessions results in forwardingToCount greater than zero` — Rename test to reflect new mapping. Assertions should check `receivingFromCount == 1`, `forwardingToCount == 0`, direction is `RECEIVING_FROM`
    2. `only TARGET sessions results in receivingFromCount greater than zero` — Rename test to reflect new mapping. Assertions should check `forwardingToCount == 1`, `receivingFromCount == 0`, direction is `FORWARDING_TO`
    3. `FORWARDING_TO represents SOURCE role` test — Update doc comment: SOURCE receives messages, so direction is RECEIVING_FROM
    4. `RECEIVING_FROM represents TARGET role` test — Update doc comment: TARGET forwards messages, so direction is FORWARDING_TO
    5. `mixed phones with different roles calculates correctly` — Swap assertion values: Phone A (SOURCE) should have RECEIVING_FROM, Phone B (TARGET) should have FORWARDING_TO. Swap forwardingToCount and receivingFromCount expected values.
    6. `multiple sessions per phone number counts each session` — SOURCE phone: change assertions from forwardingToCount=3 to receivingFromCount=3, direction from FORWARDING_TO to RECEIVING_FROM
    7. `complex scenario with multiple phones and roles` — Swap forwardingToCount and receivingFromCount expected values (SOURCE phones count as receivingFrom, TARGET phones count as forwardingTo)
    8. `archived devices should NOT appear in calculations` — SOURCE device: change from forwardingToCount=1 to receivingFromCount=1
    9. `ended sessions should NOT appear in calculations` — SOURCE device: change from forwardingToCount=1 to receivingFromCount=1
    10. `SessionWithDirection correctly associates session with device and direction` — This uses explicit Direction.FORWARDING_TO with SOURCE device. Either update to RECEIVING_FROM or leave as-is (it's testing data class construction, not mapping logic). Update to RECEIVING_FROM for consistency.
    11. `SessionWithDirection preserves session metadata` — Uses TARGET + RECEIVING_FROM. Update to TARGET + FORWARDING_TO for consistency.

    CRITICAL: Keep enum value names (FORWARDING_TO, RECEIVING_FROM) the same. Only swap which role maps to which direction.
  </action>
  <verify>
    Run: `./gradlew test --tests "dev.notyouraverage.smscourier.data.DirectionalStatusTest"`
    All tests should pass with the updated mapping.

    Then run full test suite: `./gradlew test`
    All tests should pass. Pay special attention to any compilation errors from Direction references in other test files.
  </verify>
  <done>
    - DirectionalStatusTest helper mirrors HomeViewModel's swapped mapping
    - All 15+ DirectionalStatusTest tests pass with corrected assertions
    - Full test suite passes with no regressions
  </done>
</task>

</tasks>

<verification>
1. `./gradlew test --tests "dev.notyouraverage.smscourier.viewmodels.HomeViewModelTest"` — All tests pass, including UAT HOME-01
2. `./gradlew test --tests "dev.notyouraverage.smscourier.data.DirectionalStatusTest"` — All tests pass with new mapping
3. `./gradlew test` — Full test suite passes, no regressions
4. `./gradlew spotlessCheck` — Code formatting passes
</verification>

<success_criteria>
- UAT HOME-01 test passes without any test modification
- All existing tests pass with the swapped Direction mapping
- Direction.kt doc comments accurately describe the new semantic meaning
- Both HomeViewModel and PairedDevicesViewModel use consistent SOURCE -> RECEIVING_FROM, TARGET -> FORWARDING_TO mapping
</success_criteria>

<output>
After completion, create `.planning/phases/25-home-screen-fixes/25-01-SUMMARY.md`
</output>
