---
phase: 25-home-screen-fixes
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - app/src/main/java/dev/notyouraverage/smscourier/composables/components/DirectionalStatusCard.kt
  - app/src/main/java/dev/notyouraverage/smscourier/composables/screens/HomeScreen.kt
  - app/src/main/java/dev/notyouraverage/smscourier/composables/components/SessionBreakdownBottomSheet.kt
  - app/src/main/java/dev/notyouraverage/smscourier/composables/screens/PairedDevicesScreen.kt
autonomous: false

must_haves:
  truths:
    - "DirectionalStatusCard shows 'Receiving from' and 'Forwarding to' labels (not just 'Receiving' / 'Forwarding')"
    - "Bidirectional indicator is removed; replaced by separate 'Forwarding to' and 'Receiving from' entries"
    - "On small screens (<400dp), DirectionalStatusCard switches to vertical compact text-only list"
    - "Inactive directions (zero count) are hidden on small screens"
    - "All screens use consistent 'Forwarding to' / 'Receiving from' terminology"
    - "SessionBreakdownBottomSheet shows phone numbers in detail sections"
  artifacts:
    - path: "app/src/main/java/dev/notyouraverage/smscourier/composables/components/DirectionalStatusCard.kt"
      provides: "Responsive layout with BoxWithConstraints, updated labels, no bidirectional indicator"
    - path: "app/src/main/java/dev/notyouraverage/smscourier/composables/screens/HomeScreen.kt"
      provides: "Updated ActiveSessionsTabContent and CompactSessionRow with consistent labels"
    - path: "app/src/main/java/dev/notyouraverage/smscourier/composables/components/SessionBreakdownBottomSheet.kt"
      provides: "Updated section titles with consistent terminology"
    - path: "app/src/main/java/dev/notyouraverage/smscourier/composables/screens/PairedDevicesScreen.kt"
      provides: "Updated DirectionalSubtitle, CompactDeviceRow, and tab labels with consistent terminology"
  key_links:
    - from: "DirectionalStatusCard"
      to: "HomeScreen"
      via: "Called from ActiveSessionsTabContent in HomeScreen"
      pattern: "DirectionalStatusCard"
    - from: "DirectionalSubtitle"
      to: "Direction enum"
      via: "when(direction) branch labels"
      pattern: "Direction\\.FORWARDING_TO.*Forwarding to"
---

<objective>
Update all UI components to use consistent "Forwarding to" / "Receiving from" terminology, remove the bidirectional indicator (show as two separate entries), and add responsive layout to DirectionalStatusCard for small screens (HOME-02).

Purpose: Completes the user-facing side of HOME-01 (labels match the corrected Direction mapping from Plan 01) and fixes HOME-02 (small screen layout). After this plan, both Home screen bugs are resolved.

Output: All UI screens display correct direction terminology. DirectionalStatusCard adapts to small screens.
</objective>

<execution_context>
@/Users/aanand/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aanand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-home-screen-fixes/25-01-SUMMARY.md
@app/src/main/java/dev/notyouraverage/smscourier/composables/components/DirectionalStatusCard.kt
@app/src/main/java/dev/notyouraverage/smscourier/composables/screens/HomeScreen.kt
@app/src/main/java/dev/notyouraverage/smscourier/composables/components/SessionBreakdownBottomSheet.kt
@app/src/main/java/dev/notyouraverage/smscourier/composables/screens/PairedDevicesScreen.kt
@app/src/main/java/dev/notyouraverage/smscourier/data/Direction.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update DirectionalStatusCard with responsive layout and new labels</name>
  <files>
    app/src/main/java/dev/notyouraverage/smscourier/composables/components/DirectionalStatusCard.kt
  </files>
  <action>
    Refactor DirectionalStatusCard to:

    1. **Update labels** (after Plan 01's Direction mapping swap):
       - "Receiving" -> "Receiving from" (RECEIVING_FROM = SOURCE, arrow down, primary color)
       - "Forwarding" -> "Forwarding to" (FORWARDING_TO = TARGET, arrow up, tertiary color)

    2. **Remove bidirectional indicator**: Per CONTEXT.md locked decision, bidirectional is removed from UI. The card now only shows two indicators: "Receiving from" and "Forwarding to". The bidirectionalCount parameter stays in the function signature for backward compat but is NOT rendered as a separate indicator. Instead, bidirectional sessions add to BOTH forwardingToCount and receivingFromCount (or the caller handles it). For the card, simply show the two directions.

       Actually, looking at how bidirectional is used: bidirectionalCount comes from HomeViewModel and is separate from forwardingTo/receivingFrom counts. Per the context decision "show as two separate entries: Forwarding & Receiving instead of a merged Bidirectional indicator", the simplest approach is:
       - In the card, combine: effectiveForwardingTo = forwardingToCount + bidirectionalCount
       - In the card, combine: effectiveReceivingFrom = receivingFromCount + bidirectionalCount
       - Remove the third bidirectional indicator column entirely

    3. **Add responsive layout with BoxWithConstraints**:
       - Import `androidx.compose.foundation.layout.BoxWithConstraints`
       - Wrap the Row/Column content in BoxWithConstraints
       - **Large screens (>= 400dp width)**: Show the current horizontal Row with two direction indicators — "Receiving from" and "Forwarding to" — (icons + badge + labels), grayed out when count is 0
       - **Small screens (< 400dp width)**: Show a vertical Column with compact text-only rows. Each row: just label + count, like "Forwarding to: 2" and "Receiving from: 1". No icons. Hide rows where count is 0 (save space). Use MaterialTheme.typography.bodyMedium for text.

    4. **Update component doc comment** to reflect the new behavior.

    Keep the `bidirectionalCount` parameter in the function signature to avoid breaking callers. Just don't render it as a separate item.
  </action>
  <verify>
    `./gradlew assembleDebug` compiles successfully.

    Compile-time verification of responsive layout implementation:
    - Verify BoxWithConstraints import: `grep -q "BoxWithConstraints" app/src/main/java/dev/notyouraverage/smscourier/composables/components/DirectionalStatusCard.kt`
    - Verify maxWidth usage: `grep -q "maxWidth" app/src/main/java/dev/notyouraverage/smscourier/composables/components/DirectionalStatusCard.kt`
    - Verify 400.dp breakpoint: `grep -q "400.dp" app/src/main/java/dev/notyouraverage/smscourier/composables/components/DirectionalStatusCard.kt`

    All three grep checks must succeed. If any fail, the responsive layout was not implemented correctly.

    Manually inspect the composable logic to ensure:
    - Only two direction indicators (no bidirectional)
    - Inactive directions hidden on small screens
  </verify>
  <done>
    - DirectionalStatusCard labels say "Receiving from" and "Forwarding to"
    - No separate bidirectional indicator (merged into both counts)
    - BoxWithConstraints provides responsive layout at 400dp breakpoint
    - Small screens show compact text-only list, hiding zero-count directions
    - Large screens show two direction indicators with icons, grayed out when zero
  </done>
</task>

<task type="auto">
  <name>Task 2: Update HomeScreen, SessionBreakdownBottomSheet, and PairedDevicesScreen labels</name>
  <files>
    app/src/main/java/dev/notyouraverage/smscourier/composables/screens/HomeScreen.kt
    app/src/main/java/dev/notyouraverage/smscourier/composables/components/SessionBreakdownBottomSheet.kt
    app/src/main/java/dev/notyouraverage/smscourier/composables/screens/PairedDevicesScreen.kt
  </files>
  <action>
    Update all UI text references to use consistent "Forwarding to" / "Receiving from" terminology. After Plan 01, the Direction enum mapping is swapped so FORWARDING_TO = TARGET (this device forwards to others) and RECEIVING_FROM = SOURCE (this device receives from others).

    **HomeScreen.kt** — `ActiveSessionsTabContent`:
    - Change DirectionSummary for `receivingFromCount`: label from "Receiving" to "Receiving from"
    - Change DirectionSummary for `forwardingToCount`: label from "Forwarding" to "Forwarding to"
    - Change DirectionSummary for `bidirectionalCount`: label stays "Forwarding & Receiving" (per context decision, show as text label instead of "Bidirectional")
    - `CompactSessionRow`: Update the `when(direction)` block:
      - `Direction.FORWARDING_TO` -> "You are forwarding" (TARGET role: this device sends messages outward)
      - `Direction.RECEIVING_FROM` -> "You are receiving" (SOURCE role: this device gets messages from others)
      - `Direction.BIDIRECTIONAL` -> "Forwarding & Receiving"
    - `CompactDeviceRow`: Update subtitle text per this before/after table:

      | Device Role | OLD text | NEW text |
      |-------------|----------|----------|
      | SOURCE | "Receives from you" | "You receive from them" |
      | TARGET | "Forwards to you" | "You forward to them" |

    **SessionBreakdownBottomSheet.kt** — Update section titles and colors per this before/after table:

      | Filter | OLD title | OLD color | NEW title | NEW color |
      |--------|-----------|-----------|-----------|-----------|
      | `Direction.FORWARDING_TO` | "Receiving From (N)" | primary | "Forwarding To (N)" | tertiary |
      | `Direction.RECEIVING_FROM` | "Forwarding To (N)" | tertiary | "Receiving From (N)" | primary |
      | `Direction.BIDIRECTIONAL` | "Bidirectional (N)" | secondary | "Forwarding & Receiving (N)" | secondary |

    Update inline comments to reflect new semantics.

    **PairedDevicesScreen.kt**:
    - `DirectionalSubtitle`: Update when(direction) labels:
      - `Direction.FORWARDING_TO` -> "Forwarding to - Active now" (was "Receiving - Active now") with tertiary color
      - `Direction.RECEIVING_FROM` -> "Receiving from - Active now" (was "Forwarding - Active now") with primary color
      - `Direction.BIDIRECTIONAL` -> "Forwarding & Receiving - Active now" (was "Bidirectional - Active now") with secondary color
    - Swap the colors: FORWARDING_TO = tertiary (arrow up), RECEIVING_FROM = primary (arrow down)
    - `DeviceCard` directional subtitle assignment: After Plan 01 swap, PairedDevicesViewModel.getDirectionForDevice() already returns correct mapping. No changes needed in DeviceCard logic.
    - Tab labels already say "Forward To Me" and "I Forward To" which are device-centric and correct. No change needed.

    Run `./gradlew spotlessApply` after all edits to ensure formatting compliance.
  </action>
  <verify>
    `./gradlew assembleDebug` compiles successfully.
    `./gradlew test` — all tests pass (no UI test regressions).
    `./gradlew spotlessCheck` — formatting passes.
  </verify>
  <done>
    - HomeScreen ActiveSessionsTabContent shows "Receiving from" / "Forwarding to" / "Forwarding & Receiving"
    - CompactSessionRow shows "You are forwarding" / "You are receiving" / "Forwarding & Receiving"
    - CompactDeviceRow shows "You receive from them" (SOURCE) / "You forward to them" (TARGET)
    - SessionBreakdownBottomSheet sections titled "Forwarding To (N)" (tertiary) and "Receiving From (N)" (primary) with correct colors
    - DirectionalSubtitle shows "Forwarding to - Active now" / "Receiving from - Active now" with matching colors
    - All screens use consistent terminology
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete HOME-01 and HOME-02 fixes: Direction mapping corrected in data layer, all UI labels updated to "Forwarding to" / "Receiving from", bidirectional shown as separate entries, DirectionalStatusCard responsive layout with BoxWithConstraints at 400dp breakpoint.</what-built>
  <how-to-verify>
    1. Build and install: `./gradlew installDebug`
    2. On the Home screen with a TARGET device that has an active session:
       - Verify the Active Sessions tab shows "Forwarding to" (not "Receiving")
       - Verify CompactSessionRow says "You are forwarding"
    3. On the Home screen with a SOURCE device that has an active session:
       - Verify it shows "Receiving from" (not "Forwarding")
       - Verify CompactSessionRow says "You are receiving"
    4. Tap the DirectionalStatusCard to open SessionBreakdownBottomSheet:
       - Verify sections are titled "Forwarding To (N)" and "Receiving From (N)" correctly
    5. Navigate to Paired Devices screen:
       - Verify device subtitles say "Forwarding to - Active now" or "Receiving from - Active now" as appropriate
    6. For HOME-02 small screen test:
       - Use Android Studio emulator with a small screen device (e.g., 320dp width)
       - Verify the DirectionalStatusCard switches to compact text-only list
       - Verify no text wrapping or overflow occurs
    7. Type "approved" or describe any visual issues.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `./gradlew test` — Full test suite passes including UAT HOME-01
2. `./gradlew assembleDebug` — Compiles without errors
3. `./gradlew spotlessCheck` — Formatting passes
4. Manual verification on device/emulator confirms correct labels and responsive layout
</verification>

<success_criteria>
- TARGET device Home screen shows "Forwarding to" labels
- SOURCE device Home screen shows "Receiving from" labels
- No "Bidirectional" indicator — shown as separate "Forwarding to" + "Receiving from" entries
- DirectionalStatusCard renders compact text-only list on screens < 400dp width
- All screens (Home, PairedDevices, SessionBreakdown) use consistent "Forwarding to" / "Receiving from" terminology
- No text overflow or wrapping on small screens
</success_criteria>

<output>
After completion, create `.planning/phases/25-home-screen-fixes/25-02-SUMMARY.md`
</output>
