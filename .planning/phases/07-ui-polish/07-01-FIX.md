---
phase: 07-ui-polish
plan: 07-01-FIX
type: fix
---

<objective>
Fix 2 UAT issues from plan 07-01.

Source: 07-01-ISSUES.md
Priority: 0 critical, 1 major, 1 minor
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

**Issues being fixed:**
@.planning/phases/07-ui-polish/07-01-ISSUES.md

**Original plan for reference:**
@.planning/phases/07-ui-polish/07-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix UAT-001 - "Approve and Create" button text wraps</name>
  <files>app/src/main/java/dev/notyouraverage/smscourier/composables/screens/PairingRequestsScreen.kt</files>
  <action>
In PasswordCreationSheet (line 377), shorten the button text from "Approve & Create" to just "Approve".

The button is in the ModalBottomSheet's Row at the bottom. The context (header says "Create Password", text fields are labeled "Password" and "Confirm Password") makes it clear this is a password creation flow, so repeating "Create" is redundant.

Change: `Text("Approve & Create")` → `Text("Approve")`
  </action>
  <verify>./gradlew assembleDebug succeeds. Button text fits on single line.</verify>
  <done>Button text is "Approve" - short, clear, fits on all screen sizes.</done>
</task>

<task type="auto">
  <name>Task 2: Fix UAT-002 - Revert segmented button labels</name>
  <files>app/src/main/java/dev/notyouraverage/smscourier/composables/screens/PairedDevicesScreen.kt</files>
  <action>
Revert the segmented button labels to the original more descriptive text:
- Line 179: Change "Incoming (${sourceDevices.size})" back to "Forward To Me (${sourceDevices.size})"
- Line 197: Change "Outgoing (${targetDevices.size})" back to "I Forward To (${targetDevices.size})"

Keep the existing overflow handling (maxLines = 1, overflow = TextOverflow.Ellipsis) in place - this protects against extreme edge cases.

The user preferred the original labels as they're more descriptive of what each section contains.
  </action>
  <verify>./gradlew assembleDebug succeeds. Labels show "Forward To Me (N)" and "I Forward To (N)".</verify>
  <done>Segmented button labels restored to original descriptive text with overflow protection.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All issues fixed (UAT-001 and UAT-002)
- [ ] `./gradlew assembleDebug` succeeds
- [ ] `./gradlew test` passes
- [ ] Button text fits on single line
- [ ] Original tab labels restored
</verification>

<success_criteria>
- All UAT issues from 07-01-ISSUES.md addressed
- Tests pass
- Ready for re-verification
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-polish/07-01-FIX-SUMMARY.md`
</output>
