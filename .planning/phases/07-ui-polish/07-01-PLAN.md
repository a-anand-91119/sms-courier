---
phase: 07-ui-polish
plan: 01
type: execute
---

<objective>
Fix segmented button text overflow and add service toggle loading state.

Purpose: Address core UI issues affecting user experience on smaller devices and during service transitions.
Output: PairedDevicesScreen with truncated tab labels, HomeScreen with loading feedback during service startup.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pending-state-ux/06-03-SUMMARY.md

**Key files:**
@app/src/main/java/dev/notyouraverage/smscourier/composables/screens/PairedDevicesScreen.kt
@app/src/main/java/dev/notyouraverage/smscourier/composables/screens/HomeScreen.kt
@app/src/main/java/dev/notyouraverage/smscourier/services/foreground/MasterService.kt

**Tech stack available:**
- Jetpack Compose with Material3
- StateFlow for reactive state
- MasterService foreground service with START_SELF/STOP_SELF actions

**Prior context:**
- Phase 6 complete: Pending state UX with rate limiting, dropdown menus, notification actions
- Loading patterns exist: CircularProgressIndicator with 20-24dp size, 2dp strokeWidth (see AddDeviceScreen, ForwardingControlScreen)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix segmented button text overflow</name>
  <files>app/src/main/java/dev/notyouraverage/smscourier/composables/screens/PairedDevicesScreen.kt</files>
  <action>
Shorten the segmented button labels in SingleChoiceSegmentedButtonRow (lines 161-194):
- Change "Forward To Me (N)" to "Incoming (N)"
- Change "I Forward To (N)" to "Outgoing (N)"

Additionally, add overflow handling to the Text composables inside SegmentedButton:
- Add maxLines = 1
- Add overflow = TextOverflow.Ellipsis

This prevents text wrapping/overflow on narrow screens (Samsung S25 is ~360dp wide).
  </action>
  <verify>./gradlew assembleDebug succeeds. Visual inspection: labels are shorter and will ellipsize if needed on narrow viewports.</verify>
  <done>Segmented button labels are "Incoming (N)" and "Outgoing (N)" with ellipsis overflow handling.</done>
</task>

<task type="auto">
  <name>Task 2: Add service toggle loading state</name>
  <files>app/src/main/java/dev/notyouraverage/smscourier/composables/screens/HomeScreen.kt</files>
  <action>
Add loading feedback when toggling the foreground service:

1. Add state variable for loading: `var isTogglingService by remember { mutableStateOf(false) }`

2. Modify onToggle handler (lines 125-138):
   - Set isTogglingService = true before sending intent
   - Launch a coroutine to poll MasterService.isRunning with a small delay (e.g., 100ms intervals, max 2s timeout)
   - Once MasterService.isRunning matches expected state (or timeout), set isTogglingService = false and update serviceRunning

3. In ServiceStatusCard (lines 211-299), modify the Switch to be disabled when isTogglingService is true.

4. Add a CircularProgressIndicator (size 24.dp, strokeWidth 2.dp) next to or in place of the Switch while loading. The indicator should appear inside the Row, replacing the Switch temporarily while toggling.

Use LaunchedEffect with delay() from kotlinx.coroutines for polling. Follow existing loading patterns from AddDeviceScreen.
  </action>
  <verify>./gradlew assembleDebug succeeds. Manual test: toggle service, observe brief spinner before switch state settles.</verify>
  <done>Service toggle shows loading indicator during startup/shutdown, switch disabled while toggling, state confirmed from MasterService.isRunning.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./gradlew assembleDebug` succeeds
- [ ] `./gradlew test` passes
- [ ] Segmented buttons show shorter labels with ellipsis overflow
- [ ] Service toggle shows loading feedback during transition
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- UI improvements visible on narrow screens
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-polish/07-01-SUMMARY.md`
</output>
